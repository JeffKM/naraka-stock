# ROADMAP — 나라카 증권거래소

> 개발 기간: 2026-07-11 ~ 2026-07-28 (약 2.5주) · 테스트: 07-29 ~ 07-31 · **오픈: 2026-08-01 (개장/폐장 시각 논의 중 — 현행 12:00~24:00 유지)**
> 진행 규칙: Phase 순서대로 진행, 각 Task 완료 시 체크 + 진행률 갱신. 매 Phase 종료 시 `npm run build` + `npm run lint` 통과 필수.

**전체 진행률: 75/76 (99%)** — 남은 1개: T-703 리허설(07-15~ 첫 리허설 장). 지정가 예약주문(Phase 10)은 프로덕션 db push까지 완료(2026-07-14). **섹터 개편(Phase 11)은 Plan 1~5 전 단계 완료·prod 배포까지 완료(2026-07-17, main `9774843`)**. **UI 개선(Phase 12, A~D)은 전 단계 완료·main 머지·prod 배포까지 완료(2026-07-19, PR#46·#47·#48)** — Phase D 마이그 2종 prod push 완료(토론방 API 200 검증). **API rate limit 보안 강화(Phase 13)는 구현·main 머지·prod db push까지 완료(2026-07-20, PR#57·#58)** — 마이그 `20260720030000_rate_limit` remote 반영 확인.

> **현행 기준(2026-07-17 섹터 개편 반영):** 종목 **42종 / 18섹터**(초기 8종 → 27종 → 42종), 초기 자금 **10,000,000원**(방문 보너스 100,000원), 섹터는 `sectors` 테이블로 데이터화(어드민 CRUD)·섹터 뉴스는 "참여확률" 모델. 운영 장 시간 **12:00~24:00**(어드민 config 조절, 개장 전까지 유지 — **8월 개장/폐장 시각은 논의 중**). 아래 Phase 0~10의 개별 Task 서술 중 "8종/27종·초기 100만원" 등은 **작성 당시 시점 기록**이며, 최신 사양은 Phase 11 및 본 노트를 따른다.

---

## Phase 0 — 프로젝트 기반 (6/6) ✅

- [x] **T-001** shadcn/ui 초기화 (다크 테마 기본) + 공통 의존성 설치 (zustand, @tanstack/react-query, react-hook-form, zod, lucide-react, sonner)
- [x] **T-002** Supabase 연동: 서버 전용 service-role 클라이언트 (`lib/supabase/server.ts`), `.env.example`, supabase CLI 초기화 — 커스텀 인증이라 브라우저 클라이언트 없음 (⚠️ 실제 Supabase 프로젝트 생성·키 입력은 수동 필요)
- [x] **T-003** DB 스키마 마이그레이션 작성 (전체 테이블 + RLS 전면 차단 + service_role 명시 grant) — 로컬 Supabase에서 적용·검증 완료
- [x] **T-004** 시드 데이터: 종목 8개(확정 종목명), config 기본값, 기준가(07-31), 테스트 가입 코드
- [x] **T-005** 공통 레이아웃: 하단 5탭 내비게이션, 헤더(장 상태 배지), 다크 "아기자기한 지옥" 팔레트, TanStack Query Provider
- [x] **T-006** `ApiResponse<T>` 래퍼·에러 핸들링 컨벤션(`lib/api/response.ts`), 도메인 타입(`types/`), 장 시간 유틸(`lib/market.ts`)

## Phase 1 — 인증·계정 (5/5) ✅

- [x] **T-101** 가입 플로우: 매장 코드 검증 → 닉네임(중복·욕설 필터) + 비밀번호 → 계정 생성 (`/signup`, 비밀번호 확인 포함)
- [x] **T-102** 로그인/로그아웃/세션: jose JWT httpOnly 쿠키(40일), `src/proxy.ts`(Next 16 — 구 middleware)로 보호 라우트 처리
- [x] **T-103** 가입 코드 1회용 소멸: `signup_user()` DB 함수 단일 트랜잭션 (행 잠금으로 동시 사용 방지)
- [x] **T-104** 방문 보너스: `claim_visit_bonus()` DB 함수, 1일 1회(PK 제약), 지갑 페이지에 입력 UI
- [x] **T-105** 계정 정지: `requireUser` 가드가 매 요청 DB 재확인 (로그인·me·보너스 즉시 차단, E2E 15케이스 검증)

## Phase 2 — 가격 엔진 (7/7) ✅

- [x] **T-201** 랜덤워크 경로 생성기 (`lib/engine/randomWalk.ts`): 편향 드리프트 + 등급별 변동성 + 점프 이벤트 + ±30% 클램프, 시드 RNG 순수 함수
- [x] **T-202** 일일 편향 추첨 (`lib/engine/bias.ts`): 가중 추첨 + **확률적 실현**(반전 30%·강도 20~100%, 밸런스 장치 — PRD §10)
- [x] **T-203** VI 구간 계산: 직전 틱 ±6% 급변 → 5분 정지 마킹 (기준은 시뮬레이션 후 조정 — PRD §4.3)
- [x] **T-204** 일일 배치: TS 엔진 + `apply_daily_batch()` 단일 트랜잭션(멱등 검증 완료), `/api/cron/daily-batch` (pg_cron 등록은 T-702)
- [x] **T-205** 현재가 조회 API `/api/quotes`: 틱 인덱스 계산, 장외엔 종가, 상하한·VI 표시 포함
- [x] **T-206** 금요일 배당 지급 (배치 내 통합, `last_dividend_date` 가드로 중복 방지)
- [x] **T-207** 몬테카를로 시뮬레이션 (`npm run simulate`): 뉴스추종 압도 발견 → 확률적 실현 도입으로 1위 3~10배 목표 달성

## Phase 3 — 거래 시스템 (5/5) ✅

- [x] **T-301** `execute_trade()` 단일 트랜잭션: 장 시간→CB→VI→계정(행 잠금)→잔고/보유량 검증→현재 틱 체결→수수료→기록. 서킷브레이커 체크 포함, SQL 테스트 9케이스 검증
- [x] **T-302** `/api/trade` + Zod (수량만 받음), DB 예외→ApiException 매핑
- [x] **T-303** 종목 상세 `/stocks/[code]` + 매수/매도 패널 (최대 버튼, 예상 체결액·수수료, 토스트, VI 시 비활성)
- [x] **T-304** 지갑 확장: `/api/portfolio` — 총자산·평가손익·보유 종목 (60초 갱신)
- [x] **T-305** 거래내역 `/api/trades` 페이지네이션 + `/history` (더 보기)

## Phase 4 — 시세 화면·차트 (6/6) ✅

- [x] **T-401** 시세판 홈: 전 종목 전광판 (현재가·등락률·上/下/VI 배지, 휴장 표시)
- [x] **T-402** lightweight-charts v5 도입: 일봉 캔들 + 당일 5분 라인 (`/api/stocks/[code]/chart` — 미래 틱·미래 요약 유출 서버 차단)
- [x] **T-403** 보간 연출: `usePriceWiggle` — 2.5초마다 ±0.1% 표시용 진동 (장중·비정지 시에만)
- [x] **T-404** `useQuotes` 훅: 틱 경계 정렬 폴링(+5초 여유), 장외 폴링 중단
- [x] **T-405** 서킷브레이커: quotes API `halted` 상태 + 전역 배너, 매매 버튼 비활성
- [x] **T-406** 게임 방법 페이지 (`/guide`)

## Phase 5 — 뉴스 시스템 (5/5) ✅

- [x] **T-501** 뉴스 템플릿 풀 (`lib/news/templates.ts`): 종목 8 × 편향 7등급 × 3개 = 168건 + 공시 4종 (**⚠️ 사장님 검수 필요 — 초안 상태**)
- [x] **T-502** 뉴스 생성 (`lib/news/generate.ts`, 배치 통합): 커버리지 70%, 정식 90%/찌라시 55% 적중, 페이크 찌라시 15%, 공시(±5%·상하한). 자동/수동 뉴스 분리(is_auto)로 재실행 안전
- [x] **T-503** 뉴스 피드 `/news`: 등급 배지(공시/뉴스/찌라시), 종목 필터 칩, 더 보기
- [x] **T-504** 종목 상세에 관련 뉴스 5건 연동
- [x] **T-505** 장중 조기 방향뉴스 (`generateEarlySignalNews`): 편향 이벤트 상위 2종을 장 70% 지점에 흘려, 잡뉴스 사이에서 판별·베팅하는 재미 제공. 방향 정확도 60%(노출 틱→종가), 이 종목은 후반 정식뉴스에서 제외. 몬테카를로 확정(2026-07-15): 밸런스(추종 중앙값 ≈본전)·차별화(원금손실 45%·상하 스프레드 2.2배) 동시 만족, 더 이르거나(장 중간) 더 정확하면(≥70%) 붕괴

## Phase 6 — 랭킹·어드민 (6/6) ✅

- [x] **T-601** 총자산 랭킹 `/ranking`: 상위 20명 + 내 등수 하단 고정, 정지 계정 제외
- [x] **T-602** 어드민 대시보드: 가입자·오늘 체결·거래대금·미사용 코드·방문 보너스 현황
- [x] **T-603** 가입 코드 묶음 생성(1~200, 혼동 문자 제외)·현황, 방문 코드 14일 자동 생성
- [x] **T-604** 서킷브레이커 발동/해제 + 깜짝 이벤트(남은 경로 재생성, `replace_future_ticks` 원자 교체, 장중 한정)
- [x] **T-605** 수동 뉴스 발행(is_auto=false로 배치와 분리), 유저 검색·정지/해제(자기 정지 차단)
- [x] **T-606** 권한 가드: 전 어드민 API `requireAdmin`, proxy `/admin` 보호 (⚠️ 최초 어드민 승격은 SQL 수동: `update users set is_admin=true where nickname='...'`. 이후 추가 어드민은 콘솔의 어드민 코드(ADM-) 발급으로 가입)

## Phase 7 — 운영 준비 (1/3)

- [x] **T-701** 밸런스 튜닝 완료 (1,000회 재검증 — PRD §10 목표 충족: 뉴스추종 중앙값 1.49배·최대 8.3배, 특정 전략 압도 없음). 이벤트 기간 config는 시드에 포함. 재조정 시 `npm run simulate`
- [x] **T-702** 배포 완료 (2026-07-12): https://naraka-stock.vercel.app — Supabase(suowtstolxzpnjdolfrn, ap-northeast-2) db push, 운영 데이터 마이그레이션화, env 4종, pg_cron `naraka-daily-batch`(22:00 KST) 등록, 프로덕션 배치 스모크 테스트 통과
- [ ] **T-703** 리허설 (07-29~31): 체크리스트 `docs/DEPLOY.md` §5 — 첫 자동 배치는 07-14(화) 22:00, 첫 리허설 장은 07-15(수) 15:00 (틱 생성 완료). ⚠️ 사장님 계정 가입 후 어드민 승격 필요

## Phase 8 — 토스증권 벤치마킹 UI 확장 + 장 운영 어드민화 (7/7) ✅

> 2026-07-13 그릴 세션 확정안. ⚠️ 프로덕션 반영 시 `npx supabase db push` 필요 (신규 마이그레이션 20260713060000·20260713070000·20260713080000)

- [x] **T-801** 발행주식수(`stocks.shares_outstanding`, 현실형 시총 분포: 우량 ~76%) + 지수 테이블(`market_indices`·`index_history`), 기준일 1,000pt divisor 부트스트랩, 리허설 초기화에 지수 이력 삭제 포함
- [x] **T-802** 시장 지수: 나스피(우량+일반)/나스닥(테마) 시총가중 체인 (`indexService.ts`) — 신규 상장·등급 변경 시 divisor 보정으로 지수 연속성 유지, 일일 배치가 지수 종가 기록, `/api/quotes`에 `indices` 포함, 어드민 상장 폼에 발행주식수 입력
- [x] **T-803** 종목 상세 분봉 캔들: 라인/15분/30분/1시간/일봉 탭 — 5분 틱을 클라이언트에서 OHLC 집계 (API 변경 없음)
- [x] **T-804** 종목 상세 확장: 내 보유 현황 카드(수량·평단·평가손익) + 시세 정보 표(시가·고가·저가·전일종가·상하한가·당일 거래량·시가총액)
- [x] **T-805** 홈 확장: 지수 카드 2종(스파크라인) + 내 자산 요약 카드(비로그인 시 로그인 유도) + 등락률 정렬 토글 + 오늘의 소식(공시 우선 2건)
- [x] **T-806** 장 운영 어드민화 (사장님 확정: 매일 개장·12시 개장 — 폐장은 이후 T-807에서 자정(24시)으로 연장): 하루 틱 수 가변화(장 시간 파생, 변동성·점프 √보정으로 밸런스 유지), `daily_ticks` 제약 0~287 완화, `execute_trade` config 장 시간 + 마지막 틱 폴백, 종가 "틱 83" 가정 제거(`tickService`), 어드민 콘솔 "장 운영 설정"(시간·정기 휴장 요일·임시 휴장/개장일), 리허설 초기화가 장 시간을 되돌리지 않도록 변경
- [x] **T-807** 자정 폐장(기본 12~24시) + 당일 장 시간 오버라이드: `config.market_hours_override`({date, openHour, closeHour}) — 자정 폐장 후 ~ 당일 개장 전에만 어드민 콘솔에서 "오늘만" 장 시간 변경/해제 가능, 안 바꾸면 기본대로, 날짜 지나면 자동 무시. `execute_trade`·시세/차트가 당일 유효 장 시간으로 판정 (마이그레이션 20260713080000)

## Phase 9 — UX 폴리시·운영 편의 (13/13) ✅

- [x] **T-901** 토스 벤치마킹 2차: 등락 컬러 시스템·인기 종목·종목 토론방·간편 구매·체결 이펙트·자산 카운트업 + 시세판·지갑 실시간 가격 연출 확장
- [x] **T-902** 고객센터: 버그·문의 게시판 + 운영자 답변(상태 3단계), 하단 탭 개편(거래내역→지갑 통합, 고객센터 탭 신설), 문의 작성 폼 모달 분리
- [x] **T-903** 설정 모달: 색상 모드(다크/라이트)·효과음 볼륨·보유 종목 변동 알림
- [x] **T-904** 로그인 UX: 비밀번호 한글 입력 두벌식 자동 변환, 로그인·가입 후 헤더 상태 갱신 픽스, 홈 로딩 스켈레톤
- [x] **T-905** 운영자 콘솔 5탭 분리(현황·운영·유저·관리·문의) + 가입 코드 관리 확장: 미사용 코드 전체 목록(오래된 순)·전체 복사·전체 버리기(2단계 확인)
- [x] **T-906** 시장 이벤트 개편: 서킷브레이커 시간 입력(1~60분), 깜짝 이벤트 → "시세 조정" 개명 + 적용 시간 선택(30분 단위, 기본 남은 전체) + 창 종료 후 그날 추첨 편향(`daily_summary.bias`)으로 복귀 — 몬테카를로 500회·E2E·Playwright 검증 완료
- [x] **T-907** UI 문구 이모지 전면 제거 (확정 컨벤션 — 신규 UI에도 이모지 금지)
- [x] **T-908** 시세판 정렬 탭 개편: 등록순·등락률순 → 시가총액·거래량·급상승·급하락 4개 탭 (기본 시가총액순, 표시용 재정렬)
- [x] **T-909** 계정 보안 강화: 비밀번호 정책 영문·숫자·특수문자 포함 8~16자 + 어드민 계정 정지 차단(서버 검증 + 콘솔 어드민 배지·정지 버튼 숨김)
- [x] **T-910** 글로벌 증시 그리드 배경: 전체 배경에 희미한 모눈 격자 + 시세·지수·비트 텍스트 잔상 드리프트 연출 (순수 CSS 애니메이션, 서버 컴포넌트, reduced-motion 대응)
- [x] **T-911** 장 시간 변경 시 오늘 경로 즉시 재조정(개장 전 전체 재생성 / 장중 지나간 틱·동결 구간 보존 후 잔여 재생성 / 폐장 후 초과 틱 절단) + 어드민 종목 관리 개편(검색·종목 다이얼로그로 등급 변경·시세 조정 통합, 시장 이벤트를 관리 탭으로 이동)
- [x] **T-912** 시세 조정 뉴스 정합화: 조정 시점 이후 예약 정식뉴스가 사라진 움직임을 설명해 차트와 어긋나던 문제 해결 — `replace_future_ticks`가 틱 교체와 같은 트랜잭션에서 (1) 조정 이후 미노출 자동 정식뉴스 무효화(공시·수동 찌라시 보존), (2) 창 이후 꼬리(resumeBias) 구간 실제 움직임 기준 재생성. `generateRegularNews` 임계값을 꼬리 길이에 비례 축소(scale). SQL 시나리오·TS 스케일링·HTTP E2E 검증 + 프로덕션 db push 완료
- [x] **T-913** 이벤트 밀도·뉴스추종 재조정: (1) 일일 편향 이벤트 종목 수를 고정 2~4 → **종목 수 × 15% ±1** 비례 스케일(`bias.ts` EVENT_RATIO, 27종 기준 3~5개, 0 종목 과다 완화). (2) 정식뉴스 노출을 가장 가파른 지점 → **후반부(tail, steepest·85% 중 늦은 쪽)**로 이동해 "사후 설명" 실현(`generate.ts` tailNewsTick). (3) 시뮬레이션 현실화: 뉴스추종을 시가 매수 → **뉴스 뜬 틱 매수**로 수정(`--news-timing` 인자). 기존 뉴스추종 1.5~1.9배는 완벽 내부자 가정 착시였고, 현실화·tail 적용 후 뉴스추종 0.97배 < 존버분산 1.03배로 우위 제거 확인(600런). middle 배치는 1.28배 역효과라 금지

## Phase 10 — 지정가 예약주문 (6/6) ✅

> 2026-07-14 그릴 세션 확정 (PRD §4.5). **호가창 아님 — "지정가 예약주문"만.** 장중 크론 없이 lazy 소급 정산(조건 닿은 과거 틱으로 결정론적 소급 체결)·예약 시 현금·주식 락·당일 만료가 핵심. ✅ 마이그레이션 `20260714070000_limit_orders.sql` **프로덕션 db push 완료(2026-07-14)** — remote 이력 반영 확인.

- [x] **T-1001** `orders` 테이블 + 예약 회계 마이그레이션 — 스키마(side·limit_price·reserved_cash/reserved_qty·status(pending/filled/cancelled/expired)·filled_at), RLS enable+force. **예약은 물리 이동 없이 orders 기록만** → 총자산·랭킹 불변, 취소·만료는 환불 로직 없이 상태만 변경. `execute_trade`는 "가용=잔고−예약합" 존중하도록 최소 수정. `reset_rehearsal_data`에 orders 정리 추가(장 시간은 유지)
- [x] **T-1002** 접수 RPC `place_limit_order` — 밴드밖(±30%, quoteService와 동일 prevClose) 차단, 즉시 충족 지정가는 `execute_trade` 시장가로 즉시 체결, 대기쪽만 pending 예약. 매수=금액/매도=수량+전량 스냅, 10건 상한
- [x] **T-1003** 소급 정산 RPC `settle_limit_orders(user?, at, date?, final?)` — `daily_ticks`에서 조건 닿은 첫 유효 틱(is_halted 스킵)에 **지정가 고정** 체결·백데이트, 접근 시 lazy 훅(portfolioService·listMyOrders) + 폐장 배치(`apply_daily_batch` 1.5단계) 편입, 미체결 당일 만료. 활성 CB 중 보류(해제 후 소급, 결과 동일)
- [x] **T-1004** API·조회 — `POST /api/orders`(접수)·`DELETE /api/orders?id=`(취소)·`GET /api/orders`(미체결·내역), `orderService`, `/api/portfolio`에 예약분(availableCash·availableQty) 반영
- [x] **T-1005** 주문 UI — TradePanel 시장가|지정가 탭, 매수=금액·매도=수량+비율퀵(전량/50%/25%), 지정가 입력(현재가/±나눔칩·밴드 범위·즉시 시장가 체결 안내), `MyOrdersCard`(미체결+취소·최근 내역) 지갑·종목 상세 마운트
- [x] **T-1006** 검증 — SQL 시나리오 통과(밴드밖·즉시체결·예약이 시장가 차단·지정가 고정 체결·백데이트·매도 수수료·정지틱 스킵·10건 상한·당일 만료·배치 최종정산), build/lint/tsc green. **브라우저 E2E 11/11 통과**(Playwright — 로그인·지정가탭·밴드밖 비활성·즉시 시장가 체결·대기 예약 토스트·MyOrdersCard 노출·취소·매도 지정가 예약). 내역은 체결·만료만 노출(취소 제외)로 다듬음. 밸런스: 가격 엔진 미변경이라 `npm run simulate` 구조 무관(지정가 이점은 전원 대칭 — 재튜닝 불요). **프로덕션 db push 완료(2026-07-14, remote 이력 확인).**

## Phase 11 — 섹터 개편 대작업 (5/5) ✅

> 2026-07-17 그릴 세션 확정. 브랜치 `feat/sector-overhaul` → **main 머지·prod 배포 완료(`9774843`)**. 스펙 `docs/superpowers/specs/2026-07-17-sector-overhaul-design.md`, 계획 `docs/superpowers/plans/2026-07-17-sector-overhaul-{1~5}-*.md`. Plan 단위(각 3~5 세부 태스크)로 서브에이전트 방식 실행·리뷰·verify. 개장 8/1 준비 완료.

- [x] **T-1101** (Plan 1 — 스키마·어드민) 섹터를 `sectors` 테이블로 데이터화(FK·seed 18섹터·RLS `sectors_read`), 어드민 CRUD 5함수 + `/api/admin/sectors`·`/stocks/sector` 라우트, SectorSection UI + StockDialog 섹터 드롭다운, `StockSector=string` 전환. `deleteJson` optional body 추가로 DELETE 라우트 잠복결함 해소. 커밋 `6124dcf..03e4ff2`
- [x] **T-1102** (Plan 2 — 로스터·자금) 종목 **27→42종**(신규 15종 OKSC/MHOL/BNMR/RTMC/NRKR/NRKC/NRKH/OKTL/MHRN/BNEN/NRKG/MLAB/MHTR/MLTV/OKBX insert + OKCC→food·MHBT→cosmetics·BNOC·SPCO→shipaero 재배치), 전 종목 기준가 **×10 밴드**(우량 50만~200만/일반 10만~100만/테마 1만~30만)·divisor 재부트스트랩(양 지수 1000pt), 자금 스케일(초기 **1,000만**·보너스 100만·기존유저 +900만), `SECTOR_LABEL` DB화(`StockQuote.sectorLabel`). 커밋 `e9db9ea..84497fe`. Plan 1+2를 main에 --no-ff 머지(`e14f65d`)
- [x] **T-1103** (Plan 3 — 엔진) 섹터 뉴스 **참여확률 모델**: `drawSectorEvents`(하루 0~3섹터, 분포 {0:.25,1:.35,2:.25,3:.15})·`applySectorEvents`(구성원 각자 70% 참여 시 ±15%p 가산, RNG 소비). RNG 순서 불변식(drawDailyBiases→drawSectorEvents→applySectorEvents→경로) batch·simulate 정합. 섹터 뉴스 이벤트당 1건(실현 등락 4등급 후 라벨 DB 주입), `SECTOR_NEWS_LABEL` 하드코딩 제거. 커밋 `e14f65d..acf4e65`
- [x] **T-1104** (Plan 4 — 콘텐츠) 신규 15종 힌트 **1,500개**(종목당 100개, 레벨 분포별)·`HINT_TEMPLATES` 42키, 섹터 템플릿 등급당 4→12개(`{sector}` 치환), OKTL 소개문 캐논 정합(혼백→요괴), 신규 15종 금융 용어 누수 9건 사건 서술로 치환. 커밋 `fe21000..76de0be`
- [x] **T-1105** (Plan 5 — 밸런스·배포) simulate STOCKS를 42종/신규가/1,000만으로 갱신, 몬테카를로 1000회 재검증 — 중앙값 ~10배·1위/중앙값≈6배는 **기저 편향드리프트 선재**(섹터/로스터·스케일 무관), 목표 "1위 3~10배"는 상대해석으로 충족·사장님 승인 현행 유지. prod 미적용 3종 db push → `reset_rehearsal_data`(FK fix `20260717040000`) → **main 머지 `9774843`로 코드 배포**(⚠️ 코드 배포가 batch보다 먼저) → 배치 date=07-31로 8/1 생성(6048틱·40뉴스) → 실앱 verify(힌트뉴스·코드노출0·섹터 한국어 라벨). 커밋 `80665ac`·`b151edf`

---

## Phase 12 — UI 개선: 아기자기지옥 × 토스식 × 소셜 (4/4) ✅

> 2026-07-18~19. deep-research 3축(토스식 증권 UX / 소셜 피드 / cozy horror 감성) + 코드 진단 결합. 리서치·로드맵 `docs/RESEARCH-ui-redesign.md`. 브랜치 `worktree-feat+ui-tweaks`, 서브에이전트 주도(SDD)로 Phase별 실행·리뷰·실앱 verify. 전역 컨테이너 `max-w-lg` 1컬럼 유지(레이아웃 불변).

- [x] **Phase A — 어드민 세로 나열 해소** 종목/유저/섹터 리스트 스크롤영역화+그리드, `ListingForm→ListingDialog`(헤더 버튼), admin "관리" 탭 6섹션 서브탭 분리. `layout.tsx` 불변. 커밋 `7b3277b`
- [x] **Phase B — 손님 시세 위계 개편** 홈 정렬/필터 바 sticky, 자산카드 비중게이지+총자산추세, 종목상세 상단요약 sticky 컴팩트바(콜백ref IntersectionObserver). 토스식 위계(우측정렬 숫자기둥·스파크라인·등락색). 커밋 `36d96a4`
- [x] **Phase C — 아기자기 지옥 감성 레이어** "보라밤" 배경(MarketGridBackdrop + 불씨 canvas 발광블렌드 + 안개 screen, 중앙dim/사이드살림 마스크, `--bg oklch(0.185 0.04 315)`) + 앱 마스코트 **보밤이**(빈상태·거래완료·보너스·온보딩 6곳, 다크/라이트 겸용) + 다크모드 접근성(포커스 링 대비 AA). 손그림·설명형 시그널은 "의미 없음"으로 스킵. 커밋 `c619200`·`50b06bb`·`1b42548` (PR#46·#47)
- [x] **Phase D — 소셜 피드 고도화** 댓글 **2단계 평톤 대댓글**(자기참조 `parent_id`+`deleted_at`, **묘비 삭제**로 답글 보존, 원자적 `delete_comment` plpgsql 함수로 cascade 유실 경합 제거, 두 쿼리 앱 조립로 PGRST201 회피) + 찌라시 **"미확인" 배지**. 호재/악재 투표·스티커 집계 리액션은 소셜 노이즈 관리로 제외. 뉴스 엄지업/다운은 기존(PR#43) 유지. 스펙/계획 `docs/superpowers/{specs,plans}/2026-07-19-social-nested-comments*`. 커밋 `e39d08c..8737fbf` (PR#48). **마이그 `20260719000000`·`20260719000100` prod push 완료(2026-07-19, 토론방 API 200·`replies`/`replyCount` 필드 검증). additive라 기존 댓글 자동호환, 리허설 재생성은 선택.**

---

## Phase 13 — API rate limit 보안 강화 (3/3) ✅

> 2026-07-20. "Cloudflare 붙일까" 논의에서 출발 — 상품 걸린 이벤트의 실질 위협은 인프라 공격이 아니라 **애플리케이션 로직 남용**(로그인 무차별 대입·방문코드 추측·가입 스팸)이고, 이건 앞단 프록시가 아니라 **앱/DB 레벨**에서만 막힌다고 판단. 기존 3층 방어(서버 체결가·`for update` 동시성·`visit_claims` PK 1일1회·bcrypt·계정 열거 방지) 점검 결과 돈이 새는 경로는 이미 봉쇄, 유일 공백인 rate limit만 보강. PR#57·#58, main 머지·prod 배포 완료.

- [x] **T-1301** DB 기반 rate limiter — `rate_limits` 테이블 + 원자적 `check_rate_limit(bucket, limit, window_s)` 고정 윈도우 함수(upsert 단일문·RLS enable+force). Vercel 서버리스는 인메모리 카운터가 인스턴스마다 갈려 신뢰 불가라 DB로 처리. `lib/rate-limit.ts`(`getClientIp` `x-forwarded-for` 파싱 + `enforceRateLimit`, **DB 오류 시 fail-open**), `RATE_LIMITED` 에러코드→HTTP 429. 로컬 DB 실동작 검증(한도 초과 차단·윈도우 리셋·버킷 독립)
- [x] **T-1302** 라우트 적용 — 카페 공유 WiFi(NAT) 고려해 **IP 한도는 넉넉·정밀 방어는 계정 단위**. 로그인 닉네임 10회/5분 + IP 60회/5분, 보너스 계정 20회/5분 + IP 120회/5분, 가입 IP 40회/10분. 방문·가입 코드 생성을 `Math.random`→**암호학적 난수(`node:crypto`)**로 교체, 방문코드 4→8자
- [x] **T-1303** prod 배포 — build/lint green. **⚠️ 마이그 버전 충돌 사고**: 최초 `20260720000000_rate_limit`이 기존 `20260720000000_backfill_candles`(prod 적용됨)와 같은 버전 → db push 시 조용히 스킵될 뻔(fail-open으로 rate limit 영구 무효). stale 워크트리라 선점 버전을 못 본 것이 원인. PR#58로 **`20260720030000`로 리네임**(미적용 상태라 안전) 후 push, `migration list`에서 local·remote 동기화 확인. 교훈: 마이그 타임스탬프는 `origin/main` 기준으로 확인

---

## 마일스톤

| 날짜 | 목표 |
|------|------|
| 07-14 | Phase 0~1 완료 (기반 + 인증) |
| 07-19 | Phase 2~3 완료 (엔진 + 거래) — **핵심 리스크 구간** |
| 07-24 | Phase 4~5 완료 (화면 + 뉴스) |
| 07-28 | Phase 6~7 완료 (어드민 + 배포) |
| 07-13 ~ 14 | Phase 8~10 (토스 벤치마킹·장 운영 어드민화·UX 폴리시·지정가 예약주문) |
| 07-17 | Phase 11 섹터 개편(42종/18섹터·참여확률 뉴스·자금 1,000만) — main 머지·prod 배포 |
| 07-18 ~ 19 | Phase 12 UI 개선(A 어드민·B 위계·C 감성 보라밤+마스코트·D 소셜 대댓글+미확인배지) — main 머지·prod 배포 완료(PR#46·#47·#48), Phase D 마이그 prod push 완료 |
| 07-20 | Phase 13 API rate limit 보안 강화(로그인·보너스·가입 남용 방어·방문코드 crypto 8자) — main 머지·prod db push 완료(PR#57·#58) |
| 07-29 ~ 31 | 리허설·버그픽스, 매장 코드 인쇄물 준비 |
| **08-01** | **개장** 🔔 (개장/폐장 시각 미정 — 8월 시각 논의 중, 그 전까지 12~24시 유지) |
