# ROADMAP — 나라카 증권거래소

> 개발 기간: 2026-07-11 ~ 2026-07-28 (약 2.5주) · 테스트: 07-29 ~ 07-31 · **오픈: 2026-08-01 15:00**
> 진행 규칙: Phase 순서대로 진행, 각 Task 완료 시 체크 + 진행률 갱신. 매 Phase 종료 시 `npm run build` + `npm run lint` 통과 필수.

**전체 진행률: 47/48 (98%)** — 남은 1개(T-703 리허설)는 07-15(수) 첫 리허설 장부터 진행 가능

---

## Phase 0 — 프로젝트 기반 (6/6) ✅

- [x] **T-001** shadcn/ui 초기화 (다크 테마 기본) + 공통 의존성 설치 (zustand, @tanstack/react-query, react-hook-form, zod, lucide-react, sonner)
- [x] **T-002** Supabase 연동: 서버 전용 service-role 클라이언트 (`lib/supabase/server.ts`), `.env.example`, supabase CLI 초기화 — 커스텀 인증이라 브라우저 클라이언트 없음 (⚠️ 실제 Supabase 프로젝트 생성·키 입력은 수동 필요)
- [x] **T-003** DB 스키마 마이그레이션 작성 (전체 테이블 + RLS 전면 차단 + service_role 명시 grant) — 로컬 Supabase에서 적용·검증 완료
- [x] **T-004** 시드 데이터: 종목 8개(확정 종목명), config 기본값, 기준가(07-31), 테스트 가입 코드
- [x] **T-005** 공통 레이아웃: 하단 5탭 내비게이션, 헤더(장 상태 배지), 다크 "아기자기한 지옥" 팔레트, TanStack Query Provider
- [x] **T-006** `ApiResponse<T>` 래퍼·에러 핸들링 컨벤션(`lib/api/response.ts`), 도메인 타입(`types/`), 장 시간 유틸(`lib/market.ts`)

## Phase 1 — 인증·계정 (5/5) ✅

- [x] **T-101** 가입 플로우: 매장 코드 검증 → 닉네임(중복·욕설 필터) + 비밀번호 → 계정 생성 (`/signup`, 비밀번호 확인 포함)
- [x] **T-102** 로그인/로그아웃/세션: jose JWT httpOnly 쿠키(40일), `src/proxy.ts`(Next 16 — 구 middleware)로 보호 라우트 처리
- [x] **T-103** 가입 코드 1회용 소멸: `signup_user()` DB 함수 단일 트랜잭션 (행 잠금으로 동시 사용 방지)
- [x] **T-104** 방문 보너스: `claim_visit_bonus()` DB 함수, 1일 1회(PK 제약), 지갑 페이지에 입력 UI
- [x] **T-105** 계정 정지: `requireUser` 가드가 매 요청 DB 재확인 (로그인·me·보너스 즉시 차단, E2E 15케이스 검증)

## Phase 2 — 가격 엔진 (7/7) ✅

- [x] **T-201** 랜덤워크 경로 생성기 (`lib/engine/randomWalk.ts`): 편향 드리프트 + 등급별 변동성 + 점프 이벤트 + ±30% 클램프, 시드 RNG 순수 함수
- [x] **T-202** 일일 편향 추첨 (`lib/engine/bias.ts`): 가중 추첨 + **확률적 실현**(반전 30%·강도 20~100%, 밸런스 장치 — PRD §10)
- [x] **T-203** VI 구간 계산: 직전 틱 ±6% 급변 → 5분 정지 마킹 (기준은 시뮬레이션 후 조정 — PRD §4.3)
- [x] **T-204** 일일 배치: TS 엔진 + `apply_daily_batch()` 단일 트랜잭션(멱등 검증 완료), `/api/cron/daily-batch` (pg_cron 등록은 T-702)
- [x] **T-205** 현재가 조회 API `/api/quotes`: 틱 인덱스 계산, 장외엔 종가, 상하한·VI 표시 포함
- [x] **T-206** 금요일 배당 지급 (배치 내 통합, `last_dividend_date` 가드로 중복 방지)
- [x] **T-207** 몬테카를로 시뮬레이션 (`npm run simulate`): 뉴스추종 압도 발견 → 확률적 실현 도입으로 1위 3~10배 목표 달성

## Phase 3 — 거래 시스템 (5/5) ✅

- [x] **T-301** `execute_trade()` 단일 트랜잭션: 장 시간→CB→VI→계정(행 잠금)→잔고/보유량 검증→현재 틱 체결→수수료→기록. 서킷브레이커 체크 포함, SQL 테스트 9케이스 검증
- [x] **T-302** `/api/trade` + Zod (수량만 받음), DB 예외→ApiException 매핑
- [x] **T-303** 종목 상세 `/stocks/[code]` + 매수/매도 패널 (최대 버튼, 예상 체결액·수수료, 토스트, VI 시 비활성)
- [x] **T-304** 지갑 확장: `/api/portfolio` — 총자산·평가손익·보유 종목 (60초 갱신)
- [x] **T-305** 거래내역 `/api/trades` 페이지네이션 + `/history` (더 보기)

## Phase 4 — 시세 화면·차트 (6/6) ✅

- [x] **T-401** 시세판 홈: 전 종목 전광판 (현재가·등락률·上/下/VI 배지, 휴장 표시)
- [x] **T-402** lightweight-charts v5 도입: 일봉 캔들 + 당일 5분 라인 (`/api/stocks/[code]/chart` — 미래 틱·미래 요약 유출 서버 차단)
- [x] **T-403** 보간 연출: `usePriceWiggle` — 2.5초마다 ±0.1% 표시용 진동 (장중·비정지 시에만)
- [x] **T-404** `useQuotes` 훅: 틱 경계 정렬 폴링(+5초 여유), 장외 폴링 중단
- [x] **T-405** 서킷브레이커: quotes API `halted` 상태 + 전역 배너, 매매 버튼 비활성
- [x] **T-406** 게임 방법 페이지 (`/guide`)

## Phase 5 — 뉴스 시스템 (4/4) ✅

- [x] **T-501** 뉴스 템플릿 풀 (`lib/news/templates.ts`): 종목 8 × 편향 7등급 × 3개 = 168건 + 공시 4종 (**⚠️ 사장님 검수 필요 — 초안 상태**)
- [x] **T-502** 뉴스 생성 (`lib/news/generate.ts`, 배치 통합): 커버리지 70%, 정식 90%/찌라시 55% 적중, 페이크 찌라시 15%, 공시(±5%·상하한). 자동/수동 뉴스 분리(is_auto)로 재실행 안전
- [x] **T-503** 뉴스 피드 `/news`: 등급 배지(공시/뉴스/찌라시), 종목 필터 칩, 더 보기
- [x] **T-504** 종목 상세에 관련 뉴스 5건 연동

## Phase 6 — 랭킹·어드민 (6/6) ✅

- [x] **T-601** 총자산 랭킹 `/ranking`: 상위 20명 + 내 등수 하단 고정, 정지 계정 제외
- [x] **T-602** 어드민 대시보드: 가입자·오늘 체결·거래대금·미사용 코드·방문 보너스 현황
- [x] **T-603** 가입 코드 묶음 생성(1~200, 혼동 문자 제외)·현황, 방문 코드 14일 자동 생성
- [x] **T-604** 서킷브레이커 발동/해제 + 깜짝 이벤트(남은 경로 재생성, `replace_future_ticks` 원자 교체, 장중 한정)
- [x] **T-605** 수동 뉴스 발행(is_auto=false로 배치와 분리), 유저 검색·정지/해제(자기 정지 차단)
- [x] **T-606** 권한 가드: 전 어드민 API `requireAdmin`, proxy `/admin` 보호 (⚠️ 최초 어드민 승격은 SQL 수동: `update users set is_admin=true where nickname='...'`)

## Phase 7 — 운영 준비 (1/3)

- [x] **T-701** 밸런스 튜닝 완료 (1,000회 재검증 — PRD §10 목표 충족: 뉴스추종 중앙값 1.49배·최대 8.3배, 특정 전략 압도 없음). 이벤트 기간 config는 시드에 포함. 재조정 시 `npm run simulate`
- [x] **T-702** 배포 완료 (2026-07-12): https://naraka-stock.vercel.app — Supabase(suowtstolxzpnjdolfrn, ap-northeast-2) db push, 운영 데이터 마이그레이션화, env 4종, pg_cron `naraka-daily-batch`(22:00 KST) 등록, 프로덕션 배치 스모크 테스트 통과
- [ ] **T-703** 리허설 (07-29~31): 체크리스트 `docs/DEPLOY.md` §5 — 첫 자동 배치는 07-14(화) 22:00, 첫 리허설 장은 07-15(수) 15:00 (틱 생성 완료). ⚠️ 사장님 계정 가입 후 어드민 승격 필요

## Phase 8 — 토스증권 벤치마킹 UI 확장 + 장 운영 어드민화 (6/6) ✅

> 2026-07-13 그릴 세션 확정안. ⚠️ 프로덕션 반영 시 `npx supabase db push` 필요 (신규 마이그레이션 20260713060000·20260713070000)

- [x] **T-801** 발행주식수(`stocks.shares_outstanding`, 현실형 시총 분포: 우량 ~76%) + 지수 테이블(`market_indices`·`index_history`), 기준일 1,000pt divisor 부트스트랩, 리허설 초기화에 지수 이력 삭제 포함
- [x] **T-802** 시장 지수: 나스피(우량+일반)/나스닥(테마) 시총가중 체인 (`indexService.ts`) — 신규 상장·등급 변경 시 divisor 보정으로 지수 연속성 유지, 일일 배치가 지수 종가 기록, `/api/quotes`에 `indices` 포함, 어드민 상장 폼에 발행주식수 입력
- [x] **T-803** 종목 상세 분봉 캔들: 라인/15분/30분/1시간/일봉 탭 — 5분 틱을 클라이언트에서 OHLC 집계 (API 변경 없음)
- [x] **T-804** 종목 상세 확장: 내 보유 현황 카드(수량·평단·평가손익) + 시세 정보 표(시가·고가·저가·전일종가·상하한가·당일 거래량·시가총액)
- [x] **T-805** 홈 확장: 지수 카드 2종(스파크라인) + 내 자산 요약 카드(비로그인 시 로그인 유도) + 등락률 정렬 토글 + 오늘의 소식(공시 우선 2건)
- [x] **T-806** 장 운영 어드민화 (사장님 확정: 매일 개장·12~22시): 하루 틱 수 가변화(장 시간 파생, 변동성·점프 √보정으로 밸런스 유지), `daily_ticks` 제약 0~287 완화, `execute_trade` config 장 시간 + 마지막 틱 폴백, 종가 "틱 83" 가정 제거(`tickService`), 어드민 콘솔 "장 운영 설정"(시간·정기 휴장 요일·임시 휴장/개장일), 리허설 초기화가 장 시간을 되돌리지 않도록 변경

---

## 마일스톤

| 날짜 | 목표 |
|------|------|
| 07-14 | Phase 0~1 완료 (기반 + 인증) |
| 07-19 | Phase 2~3 완료 (엔진 + 거래) — **핵심 리스크 구간** |
| 07-24 | Phase 4~5 완료 (화면 + 뉴스) |
| 07-28 | Phase 6~7 완료 (어드민 + 배포) |
| 07-29 ~ 31 | 리허설·버그픽스, 매장 코드 인쇄물 준비 |
| **08-01 15:00** | **개장** 🔔 |
