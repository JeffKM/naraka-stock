# 스티커 기능 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 종목 토론방 댓글에 제작진 큐레이션 스티커(캐릭터 짤 등)를 붙일 수 있게 하고, 어드민이 배포 없이 스티커를 추가·관리할 수 있게 한다.

**Architecture:** 스티커 카탈로그를 신규 `stickers` DB 테이블에 이미지 data URI(base64)로 저장(1단계). `stock_comments`에 `sticker_id` 컬럼을 더하고 content를 nullable로 완화해 "텍스트·스티커 중 최소 하나"를 허용한다. 댓글 응답에는 `stickerId`만 싣고, 이미지는 `GET /api/stickers` 카탈로그를 클라이언트가 1회 캐시해 id→imageUrl로 매핑한다(payload 최소화 + 2단계 Storage 이관 대비). 어드민 콘솔에 스티커 관리 섹션을 추가한다.

**Tech Stack:** Next.js 16 (App Router) + React 19, TypeScript strict, Supabase(Postgres + RLS), TanStack Query v5, Zod v4, shadcn/ui(new-york), lucide-react.

## Global Constraints

- 스펙: `docs/superpowers/specs/2026-07-18-stickers-design.md` (이 계획의 단일 출처).
- 돈 계산 아님 — 스티커는 현금가치 0, 밸런스·시뮬 영향 없음(`simulate` 재검증 불필요).
- TypeScript strict, `any` 금지. 들여쓰기 2칸, 세미콜론, 더블 쿼트.
- 임포트: 개별 임포트(lucide-react 포함), 경로 alias `@/*` → `./src/*`.
- API 응답은 `ApiResponse<T>` 래퍼(`apiOk`/`apiError`/`handleApiError`), 에러는 `ApiException`.
- 변수/함수 camelCase, 컴포넌트 PascalCase, 테이블/컬럼 snake_case.
- 코드 주석·커밋 메시지 한국어. 커밋 형식 `type: 한국어 설명`.
- **유닛 테스트 프레임워크 없음** — 각 태스크 검증은 `npm run build` + `npm run lint` 통과를 게이트로 하고, API는 `curl`, UI는 실앱(verify 스킬)로 확인한다.
- data URI 개당 상한 **100KB**. 허용 형식 png/jpeg/webp/svg.
- 유니코드 이모지 금지 규칙([[no-emoji-in-ui]])은 유지하되 스티커는 이미지 자산이라 별개. 아이콘은 lucide 컴포넌트 사용.
- 커밋 끝에 한 줄: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

## File Structure

- `supabase/migrations/20260718080000_stickers.sql` — 신규. `stickers` 테이블 + `stock_comments` 확장.
- `supabase/seed.sql` — 수정. 로컬/리허설용 임시 스티커 1개.
- `src/services/stickerService.ts` — 신규. 카탈로그 CRUD + 검증 + `assertValidStickerId`.
- `src/services/commentService.ts` — 수정. `createComment`에 `stickerId`, DTO/select에 `sticker_id`.
- `src/app/api/stickers/route.ts` — 신규. 공개 카탈로그 GET.
- `src/app/api/admin/stickers/route.ts` — 신규. 어드민 CRUD.
- `src/app/api/stocks/[code]/comments/route.ts` — 수정. POST Zod에 `stickerId`, content optional.
- `src/components/ui/popover.tsx` — 신규(shadcn). `@radix-ui/react-popover` 의존.
- `src/hooks/useStickers.ts` — 신규. 카탈로그 훅(id→imageUrl 맵).
- `src/components/trade/StickerPicker.tsx` — 신규. 스티커 선택 팝오버.
- `src/components/trade/StockComments.tsx` — 수정. 피커 통합 + 스티커 렌더 + 단독 전송.
- `src/components/news/DiscussionList.tsx` — 수정. 토론뷰 스티커 렌더.
- `src/components/admin/StickerSection.tsx` — 신규. 어드민 관리 섹션.
- `src/app/admin/page.tsx` — 수정. StickerSection 편입.

**리허설 reset 무수정:** `reset_rehearsal_data`는 `stickers`를 건드리지 않는다(카탈로그는 어드민 자산이라 보존). 유저 댓글은 유저 cascade로 사라지고 `stickers` 행은 남는다. 별도 함수 수정 불필요.

---

### Task 1: 마이그레이션 — stickers 테이블 + stock_comments 확장

**Files:**
- Create: `supabase/migrations/20260718080000_stickers.sql`
- Modify: `supabase/seed.sql` (끝에 추가)

**Interfaces:**
- Produces: 테이블 `stickers(id text pk, label text, image_data_uri text, sort_order int, is_active bool, created_at)`, 컬럼 `stock_comments.sticker_id text null`, 제약 `stock_comments_content_len`·`stock_comments_has_body`.

- [ ] **Step 1: 마이그레이션 작성**

`supabase/migrations/20260718080000_stickers.sql`:

```sql
-- 스티커 카탈로그 + 댓글 첨부 (몰입 스펙 §2-4)
-- 1단계: 이미지를 data URI(base64)로 DB에 직접 저장. 어드민에서 배포 없이 추가.
-- 2단계(대량화 시): storage_path 추가로 Supabase Storage 이관 — 소비 코드는 imageUrl만 봄.

create table stickers (
  id text primary key,
  label text not null,
  image_data_uri text not null,
  sort_order int not null default 0,
  is_active boolean not null default true,
  created_at timestamptz not null default now()
);

create index stickers_active_idx on stickers (is_active, sort_order);

-- 다른 테이블과 동일하게 RLS 전면 차단 (service role만 통과)
alter table stickers enable row level security;
alter table stickers force row level security;

-- 댓글에 스티커 첨부: 없으면 텍스트만, 있으면 텍스트 생략 가능
-- 스티커 하드 삭제 시 기존 댓글은 텍스트만 남기고 깨지지 않도록 set null
alter table stock_comments
  add column sticker_id text null references stickers (id) on delete set null;

-- content NOT NULL 완화 + 길이 체크 재정의
-- (원본은 인라인 check → Postgres 자동명 stock_comments_content_check)
alter table stock_comments alter column content drop not null;
alter table stock_comments drop constraint if exists stock_comments_content_check;
alter table stock_comments add constraint stock_comments_content_len
  check (content is null or char_length(content) between 1 and 200);

-- 텍스트·스티커 중 최소 하나는 필수
alter table stock_comments add constraint stock_comments_has_body
  check (content is not null or sticker_id is not null);
```

- [ ] **Step 2: seed에 임시 스티커 1개 추가**

`supabase/seed.sql` 맨 끝에 추가(로컬/리허설 전용 — 프로덕션엔 어드민으로 실제 스티커 추가):

```sql
-- 임시 스티커 1개 (로컬/리허설 테스트용, 실제 캐릭터 짤은 어드민에서 추가)
insert into stickers (id, label, image_data_uri, sort_order) values
  ('placeholder-01', '임시 스티커',
   'data:image/svg+xml;base64,' || replace(encode(
     '<svg xmlns="http://www.w3.org/2000/svg" width="96" height="96"><rect width="96" height="96" rx="16" fill="#7c3aed"/><text x="48" y="62" font-size="44" text-anchor="middle" fill="#fff">*</text></svg>'::bytea,
     'base64'), chr(10), ''),
   0);
```

- [ ] **Step 3: DB 리셋으로 마이그레이션·seed 적용 검증**

Run: `npx supabase db reset`
Expected: 에러 없이 완료. 이어서 검증:

Run: `npx supabase db reset && psql "$(npx supabase status -o env | grep DB_URL | cut -d= -f2- | tr -d '\"')" -c "\d stock_comments" -c "select id, is_active from stickers;"`
Expected: `stock_comments`에 `sticker_id` 컬럼과 두 제약이 보이고, `stickers`에 `placeholder-01` 1행.
(psql 접속이 번거로우면 최소한 `npx supabase db reset`가 무오류로 끝나는 것으로 갈음.)

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/20260718080000_stickers.sql supabase/seed.sql
git commit -m "feat: 스티커 카탈로그 테이블·댓글 sticker_id 컬럼 마이그레이션"
```

---

### Task 2: stickerService — 카탈로그 CRUD + 검증

**Files:**
- Create: `src/services/stickerService.ts`

**Interfaces:**
- Consumes: `getSupabaseAdmin()`(`@/lib/supabase/server`), `ApiException`(`@/lib/api/response`), Task 1의 `stickers` 테이블.
- Produces:
  - `interface Sticker { id: string; label: string; imageUrl: string; sortOrder: number; isActive: boolean }`
  - `interface StickerInput { id: string; label: string; imageDataUri: string; sortOrder?: number }`
  - `listStickers(activeOnly: boolean): Promise<Sticker[]>`
  - `createSticker(input: StickerInput): Promise<void>`
  - `updateSticker(id: string, patch: { label?: string; sortOrder?: number; imageDataUri?: string }): Promise<void>`
  - `setStickerActive(id: string, active: boolean): Promise<void>`
  - `deleteSticker(id: string): Promise<void>`
  - `assertValidStickerId(id: string): Promise<void>`

- [ ] **Step 1: 서비스 작성**

`src/services/stickerService.ts`:

```ts
import "server-only";
import { ApiException } from "@/lib/api/response";
import { getSupabaseAdmin } from "@/lib/supabase/server";

// 스티커 카탈로그 — 어드민이 관리, 유저 댓글에 첨부.
// 1단계는 이미지를 data URI로 DB에 저장한다. imageUrl 추상화로 2단계(Storage) 이관 대비.

export interface Sticker {
  id: string;
  label: string;
  imageUrl: string; // 1단계: data URI, 2단계: Storage 공개 URL
  sortOrder: number;
  isActive: boolean;
}

export interface StickerInput {
  id: string;
  label: string;
  imageDataUri: string;
  sortOrder?: number;
}

const SLUG_RE = /^[a-z0-9][a-z0-9-]{0,63}$/;
const DATA_URI_RE = /^data:image\/(png|jpe?g|webp|svg\+xml);base64,/;
const MAX_STICKER_BYTES = 100 * 1024; // 개당 100KB 상한

interface StickerRow {
  id: string;
  label: string;
  image_data_uri: string;
  sort_order: number;
  is_active: boolean;
}

function toSticker(row: StickerRow): Sticker {
  return {
    id: row.id,
    label: row.label,
    imageUrl: row.image_data_uri,
    sortOrder: row.sort_order,
    isActive: row.is_active,
  };
}

function validateDataUri(imageDataUri: string): void {
  if (!DATA_URI_RE.test(imageDataUri)) {
    throw new ApiException("VALIDATION", "지원하지 않는 이미지 형식입니다. (png/jpeg/webp/svg)");
  }
  const base64 = imageDataUri.slice(imageDataUri.indexOf(",") + 1);
  const bytes = Buffer.from(base64, "base64").length;
  if (bytes > MAX_STICKER_BYTES) {
    throw new ApiException("VALIDATION", "스티커 이미지는 100KB 이하만 올릴 수 있어요.");
  }
}

export async function listStickers(activeOnly: boolean): Promise<Sticker[]> {
  const supabase = getSupabaseAdmin();
  let query = supabase
    .from("stickers")
    .select("id, label, image_data_uri, sort_order, is_active")
    .order("sort_order", { ascending: true })
    .order("created_at", { ascending: true });
  if (activeOnly) query = query.eq("is_active", true);
  const { data, error } = await query;
  if (error) throw error;
  return (data as StickerRow[]).map(toSticker);
}

export async function createSticker(input: StickerInput): Promise<void> {
  if (!SLUG_RE.test(input.id)) {
    throw new ApiException("VALIDATION", "id는 영소문자·숫자·하이픈만 가능합니다.");
  }
  if (input.label.trim().length === 0) {
    throw new ApiException("VALIDATION", "라벨을 입력해주세요.");
  }
  validateDataUri(input.imageDataUri);
  const supabase = getSupabaseAdmin();
  const { error } = await supabase.from("stickers").insert({
    id: input.id,
    label: input.label.trim(),
    image_data_uri: input.imageDataUri,
    sort_order: input.sortOrder ?? 0,
  });
  if (error) {
    if (error.code === "23505") throw new ApiException("VALIDATION", "이미 있는 스티커 id입니다.");
    throw error;
  }
}

export async function updateSticker(
  id: string,
  patch: { label?: string; sortOrder?: number; imageDataUri?: string }
): Promise<void> {
  const update: Record<string, unknown> = {};
  if (patch.label !== undefined) {
    if (patch.label.trim().length === 0) throw new ApiException("VALIDATION", "라벨을 입력해주세요.");
    update.label = patch.label.trim();
  }
  if (patch.sortOrder !== undefined) update.sort_order = patch.sortOrder;
  if (patch.imageDataUri !== undefined) {
    validateDataUri(patch.imageDataUri);
    update.image_data_uri = patch.imageDataUri;
  }
  if (Object.keys(update).length === 0) return;
  const supabase = getSupabaseAdmin();
  const { data, error } = await supabase
    .from("stickers")
    .update(update)
    .eq("id", id)
    .select("id")
    .maybeSingle();
  if (error) throw error;
  if (!data) throw new ApiException("NOT_FOUND", "없는 스티커입니다.");
}

export async function setStickerActive(id: string, active: boolean): Promise<void> {
  const supabase = getSupabaseAdmin();
  const { data, error } = await supabase
    .from("stickers")
    .update({ is_active: active })
    .eq("id", id)
    .select("id")
    .maybeSingle();
  if (error) throw error;
  if (!data) throw new ApiException("NOT_FOUND", "없는 스티커입니다.");
}

export async function deleteSticker(id: string): Promise<void> {
  const supabase = getSupabaseAdmin();
  const { data, error } = await supabase
    .from("stickers")
    .delete()
    .eq("id", id)
    .select("id")
    .maybeSingle();
  if (error) throw error;
  if (!data) throw new ApiException("NOT_FOUND", "없는 스티커입니다.");
}

// 댓글 작성 시 첨부 스티커가 실재하고 활성인지 검증한다.
export async function assertValidStickerId(id: string): Promise<void> {
  const supabase = getSupabaseAdmin();
  const { data, error } = await supabase
    .from("stickers")
    .select("id")
    .eq("id", id)
    .eq("is_active", true)
    .maybeSingle();
  if (error) throw error;
  if (!data) throw new ApiException("VALIDATION", "사용할 수 없는 스티커입니다.");
}
```

- [ ] **Step 2: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과(타입 에러·린트 에러 없음).

- [ ] **Step 3: 커밋**

```bash
git add src/services/stickerService.ts
git commit -m "feat: 스티커 카탈로그 서비스(CRUD·data URI 검증·유효성) 추가"
```

---

### Task 3: 공개 카탈로그 API — GET /api/stickers

**Files:**
- Create: `src/app/api/stickers/route.ts`

**Interfaces:**
- Consumes: `listStickers`(Task 2), `apiOk`/`handleApiError`(`@/lib/api/response`).
- Produces: `GET /api/stickers` → `{ stickers: { id, label, imageUrl }[] }` (활성만).

- [ ] **Step 1: 라우트 작성**

`src/app/api/stickers/route.ts`:

```ts
import { apiOk, handleApiError } from "@/lib/api/response";
import { listStickers } from "@/services/stickerService";

// 공개 스티커 카탈로그(활성만). 클라이언트가 1회 캐시해 댓글의 sticker_id를 이미지로 매핑한다.
export async function GET() {
  try {
    const stickers = await listStickers(true);
    return apiOk({
      stickers: stickers.map((s) => ({ id: s.id, label: s.label, imageUrl: s.imageUrl })),
    });
  } catch (error) {
    return handleApiError(error);
  }
}
```

- [ ] **Step 2: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 3: 실제 응답 확인**

Run: `npm run dev` 백그라운드 후 `curl -s localhost:3000/api/stickers`
Expected: `{"success":true,"data":{"stickers":[{"id":"placeholder-01","label":"임시 스티커","imageUrl":"data:image/svg+xml;base64,..."}]}}`
(로컬 DB에 seed가 적용돼 있어야 함 — Task 1 Step 3.)

- [ ] **Step 4: 커밋**

```bash
git add src/app/api/stickers/route.ts
git commit -m "feat: 공개 스티커 카탈로그 API(GET /api/stickers) 추가"
```

---

### Task 4: commentService 확장 — 스티커 첨부

**Files:**
- Modify: `src/services/commentService.ts`

**Interfaces:**
- Consumes: `assertValidStickerId`(Task 2).
- Produces:
  - `createComment(userId: number, stockCode: string, content: string | null, stickerId: string | null): Promise<void>` (시그니처 변경)
  - `StockComment`에 `stickerId: string | null`
  - `DiscussionComment`는 `StockComment` 확장이라 자동 포함

- [ ] **Step 1: import 추가**

`src/services/commentService.ts` 상단 import에 추가:

```ts
import { assertValidStickerId } from "@/services/stickerService";
```

- [ ] **Step 2: StockComment 인터페이스에 stickerId 추가**

`export interface StockComment { ... }` 안, `likedByMe` 다음 줄에 추가:

```ts
  stickerId: string | null; // 첨부 스티커 id (없으면 null)
```

- [ ] **Step 3: listComments select·매핑에 sticker_id 반영**

`listComments`의 `.select(...)` 문자열에 `sticker_id`를 추가:

```ts
    .select("id, user_id, content, created_at, sticker_id, users!stock_comments_user_id_fkey(nickname)")
```

같은 함수 `return data.map(...)`의 반환 객체에 추가(`likedByMe` 다음):

```ts
      stickerId: row.sticker_id ?? null,
```

- [ ] **Step 4: listAllComments select·매핑에 sticker_id 반영**

`listAllComments`의 `.select(...)`에 `sticker_id` 추가:

```ts
      "id, user_id, stock_code, content, created_at, sticker_id, users!stock_comments_user_id_fkey(nickname), stocks(name)"
```

같은 함수 반환 객체에 추가(`likedByMe` 다음):

```ts
      stickerId: row.sticker_id ?? null,
```

- [ ] **Step 5: createComment 시그니처·검증·insert 변경**

기존 `createComment`를 다음으로 교체:

```ts
export async function createComment(
  userId: number,
  stockCode: string,
  content: string | null,
  stickerId: string | null
): Promise<void> {
  const supabase = getSupabaseAdmin();

  // 텍스트·스티커 중 최소 하나는 있어야 한다 (API에서도 막지만 서비스에서 재확인)
  if (!content && !stickerId) {
    throw new ApiException("VALIDATION", "내용이나 스티커를 입력해주세요.");
  }

  const { data: stock, error: stockError } = await supabase
    .from("stocks")
    .select("code")
    .eq("code", stockCode)
    .maybeSingle();
  if (stockError) throw stockError;
  if (!stock) {
    throw new ApiException("NOT_FOUND", "없는 종목입니다.");
  }

  if (stickerId) await assertValidStickerId(stickerId);

  // 도배 방지: 최근 1분 작성 수 제한
  const since = new Date(Date.now() - 60_000).toISOString();
  const { count, error: countError } = await supabase
    .from("stock_comments")
    .select("id", { count: "exact", head: true })
    .eq("user_id", userId)
    .gte("created_at", since);
  if (countError) throw countError;
  if ((count ?? 0) >= BURST_LIMIT) {
    throw new ApiException("VALIDATION", "너무 빨라요! 잠시 후 다시 남겨주세요.");
  }

  const { error } = await supabase
    .from("stock_comments")
    .insert({ user_id: userId, stock_code: stockCode, content, sticker_id: stickerId });
  if (error) throw error;
}
```

(`updateComment`은 텍스트 전용 수정 유지 — 변경 없음. 스티커 단독 댓글은 수정 UI에서 연필 버튼을 숨긴다(Task 8).)

- [ ] **Step 6: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과. (호출부 `route.ts`는 Task 5에서 맞춘다 — 이 태스크만 빌드하면 인자 개수 불일치로 실패할 수 있으니 Task 5와 이어서 진행하고, 최종 빌드는 Task 5 Step 끝에서 확인한다.)

- [ ] **Step 7: 커밋**

```bash
git add src/services/commentService.ts
git commit -m "feat: 댓글 서비스에 스티커 첨부(sticker_id) 반영"
```

---

### Task 5: 댓글 API 확장 — POST에 stickerId, content optional

**Files:**
- Modify: `src/app/api/stocks/[code]/comments/route.ts`

**Interfaces:**
- Consumes: 변경된 `createComment`(Task 4).
- Produces: `POST /api/stocks/[code]/comments` body `{ content?: string; stickerId?: string }` (최소 하나 필수).

- [ ] **Step 1: Zod 스키마·POST 핸들러 수정**

`createSchema` 정의와 `POST` 함수를 다음으로 교체(기존 `contentField`는 유지):

```ts
const stickerIdField = z.string().trim().min(1).max(64);

const createSchema = z
  .object({
    content: contentField.optional(),
    stickerId: stickerIdField.optional(),
  })
  .refine((d) => Boolean(d.content) || Boolean(d.stickerId), {
    message: "내용이나 스티커를 입력해주세요",
  });
```

```ts
// 댓글 작성 (텍스트·스티커 중 최소 하나)
export async function POST(request: Request, { params }: RouteContext) {
  try {
    const user = await requireUser();
    const { code } = await params;
    const parsed = createSchema.safeParse(await request.json());
    if (!parsed.success) {
      return apiError("VALIDATION", parsed.error.issues[0].message);
    }
    await createComment(
      user.id,
      code.toUpperCase(),
      parsed.data.content ?? null,
      parsed.data.stickerId ?? null
    );
    return apiOk({ ok: true });
  } catch (error) {
    return handleApiError(error);
  }
}
```

(`updateSchema`·`PATCH`·`DELETE`·`GET`는 변경 없음.)

- [ ] **Step 2: 빌드·린트 (Task 4+5 통합 검증)**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 3: API 동작 확인 (스티커 단독 + 텍스트+스티커)**

로그인 세션 쿠키가 필요하므로 실앱 verify(Task 8)에서 함께 확인해도 된다. 간이 확인:

Run: `curl -s -X POST localhost:3000/api/stocks/<CODE>/comments -H 'Content-Type: application/json' -d '{}'`
Expected: `{"success":false,"error":{"code":"VALIDATION",...}}` (내용·스티커 둘 다 없음) 또는 `UNAUTHORIZED`(비로그인). 어느 쪽이든 400/401 계열.

- [ ] **Step 4: 커밋**

```bash
git add "src/app/api/stocks/[code]/comments/route.ts"
git commit -m "feat: 댓글 작성 API에 stickerId 수용·content 선택화"
```

---

### Task 6: 어드민 스티커 CRUD API

**Files:**
- Create: `src/app/api/admin/stickers/route.ts`

**Interfaces:**
- Consumes: `requireAdmin`(`@/lib/auth/guards`), `listStickers`/`createSticker`/`updateSticker`/`setStickerActive`/`deleteSticker`(Task 2).
- Produces:
  - `GET /api/admin/stickers` → `{ stickers: Sticker[] }` (전체, 비활성 포함)
  - `POST` body `{ id, label, imageDataUri, sortOrder? }`
  - `PATCH` body `{ id, label?, sortOrder?, imageDataUri?, isActive? }`
  - `DELETE ?id=`

- [ ] **Step 1: 라우트 작성**

`src/app/api/admin/stickers/route.ts`:

```ts
import { z } from "zod";
import { apiError, apiOk, handleApiError } from "@/lib/api/response";
import { requireAdmin } from "@/lib/auth/guards";
import {
  createSticker,
  deleteSticker,
  listStickers,
  setStickerActive,
  updateSticker,
} from "@/services/stickerService";

// 어드민 스티커 관리 — 최종 권한 검증은 requireAdmin이 담당.
export async function GET() {
  try {
    await requireAdmin();
    return apiOk({ stickers: await listStickers(false) });
  } catch (error) {
    return handleApiError(error);
  }
}

const createSchema = z.object({
  id: z.string().trim().min(1).max(64),
  label: z.string().trim().min(1).max(60),
  imageDataUri: z.string().min(1),
  sortOrder: z.number().int().optional(),
});

export async function POST(request: Request) {
  try {
    await requireAdmin();
    const parsed = createSchema.safeParse(await request.json());
    if (!parsed.success) {
      return apiError("VALIDATION", parsed.error.issues[0].message);
    }
    await createSticker(parsed.data);
    return apiOk({ ok: true });
  } catch (error) {
    return handleApiError(error);
  }
}

const patchSchema = z.object({
  id: z.string().trim().min(1),
  label: z.string().trim().min(1).max(60).optional(),
  sortOrder: z.number().int().optional(),
  imageDataUri: z.string().min(1).optional(),
  isActive: z.boolean().optional(),
});

export async function PATCH(request: Request) {
  try {
    await requireAdmin();
    const parsed = patchSchema.safeParse(await request.json());
    if (!parsed.success) {
      return apiError("VALIDATION", parsed.error.issues[0].message);
    }
    const { id, isActive, label, sortOrder, imageDataUri } = parsed.data;
    if (isActive !== undefined) await setStickerActive(id, isActive);
    if (label !== undefined || sortOrder !== undefined || imageDataUri !== undefined) {
      await updateSticker(id, { label, sortOrder, imageDataUri });
    }
    return apiOk({ ok: true });
  } catch (error) {
    return handleApiError(error);
  }
}

export async function DELETE(request: Request) {
  try {
    await requireAdmin();
    const id = new URL(request.url).searchParams.get("id");
    if (!id) return apiError("VALIDATION", "삭제할 스티커 id가 필요합니다.");
    await deleteSticker(id);
    return apiOk({ ok: true });
  } catch (error) {
    return handleApiError(error);
  }
}
```

- [ ] **Step 2: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 3: 커밋**

```bash
git add src/app/api/admin/stickers/route.ts
git commit -m "feat: 어드민 스티커 CRUD API 추가"
```

---

### Task 7: Popover UI + useStickers 훅 + StickerPicker

**Files:**
- Create: `src/components/ui/popover.tsx` (shadcn)
- Create: `src/hooks/useStickers.ts`
- Create: `src/components/trade/StickerPicker.tsx`
- Modify: `package.json` (의존성 `@radix-ui/react-popover`)

**Interfaces:**
- Consumes: `GET /api/stickers`(Task 3).
- Produces:
  - `interface CatalogSticker { id: string; label: string; imageUrl: string }`
  - `useStickers(): { stickers: CatalogSticker[]; byId: Map<string, CatalogSticker> }`
  - `<StickerPicker onSelect={(s: CatalogSticker) => void} />`

- [ ] **Step 1: Popover 의존성 설치**

Run: `npm install @radix-ui/react-popover`
Expected: `package.json`·`package-lock.json`에 `@radix-ui/react-popover` 추가.

- [ ] **Step 2: shadcn Popover 컴포넌트 작성**

`src/components/ui/popover.tsx` (new-york 스타일, 기존 `dialog.tsx`와 동일 관례):

```tsx
"use client";

import * as PopoverPrimitive from "@radix-ui/react-popover";
import type { ComponentProps } from "react";
import { cn } from "@/lib/utils";

function Popover(props: ComponentProps<typeof PopoverPrimitive.Root>) {
  return <PopoverPrimitive.Root data-slot="popover" {...props} />;
}

function PopoverTrigger(props: ComponentProps<typeof PopoverPrimitive.Trigger>) {
  return <PopoverPrimitive.Trigger data-slot="popover-trigger" {...props} />;
}

function PopoverContent({
  className,
  align = "center",
  sideOffset = 4,
  ...props
}: ComponentProps<typeof PopoverPrimitive.Content>) {
  return (
    <PopoverPrimitive.Portal>
      <PopoverPrimitive.Content
        data-slot="popover-content"
        align={align}
        sideOffset={sideOffset}
        className={cn(
          "z-50 w-72 rounded-md border bg-popover p-4 text-popover-foreground shadow-md outline-none",
          className
        )}
        {...props}
      />
    </PopoverPrimitive.Portal>
  );
}

export { Popover, PopoverTrigger, PopoverContent };
```

Note: `bg-popover`/`text-popover-foreground` 토큰이 테마에 없으면 `dialog.tsx`가 쓰는 토큰(`bg-card`/`text-foreground`)으로 대체한다. 빌드 후 팝오버 배경이 투명하면 이 토큰을 확인.

- [ ] **Step 3: useStickers 훅 작성**

`src/hooks/useStickers.ts`:

```ts
"use client";

import { useQuery } from "@tanstack/react-query";
import { useMemo } from "react";
import { getJson } from "@/lib/api/client";

export interface CatalogSticker {
  id: string;
  label: string;
  imageUrl: string;
}

// 공개 스티커 카탈로그를 1회 로드해 캐시한다. 댓글의 sticker_id를 이미지로 매핑할 때 쓴다.
export function useStickers() {
  const { data } = useQuery({
    queryKey: ["stickers"],
    queryFn: () => getJson<{ stickers: CatalogSticker[] }>("/api/stickers"),
    staleTime: 5 * 60_000,
  });
  const stickers = useMemo(() => data?.stickers ?? [], [data]);
  const byId = useMemo(() => new Map(stickers.map((s) => [s.id, s])), [stickers]);
  return { stickers, byId };
}
```

- [ ] **Step 4: StickerPicker 작성**

`src/components/trade/StickerPicker.tsx`:

```tsx
"use client";

import { useState } from "react";
import { Smile } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { useStickers, type CatalogSticker } from "@/hooks/useStickers";

// 입력창 옆 스티커 선택 팝오버. 선택 시 onSelect로 스티커를 올려보내고 닫는다.
export function StickerPicker({ onSelect }: { onSelect: (sticker: CatalogSticker) => void }) {
  const { stickers } = useStickers();
  const [open, setOpen] = useState(false);

  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button type="button" variant="outline" size="icon" aria-label="스티커 선택">
          <Smile className="size-4" />
        </Button>
      </PopoverTrigger>
      <PopoverContent align="end" className="w-64 p-2">
        {stickers.length === 0 ? (
          <p className="py-4 text-center text-xs text-muted-foreground">등록된 스티커가 없어요</p>
        ) : (
          <div className="grid grid-cols-4 gap-1">
            {stickers.map((s) => (
              <button
                key={s.id}
                type="button"
                onClick={() => {
                  onSelect(s);
                  setOpen(false);
                }}
                aria-label={s.label}
                className="aspect-square rounded-md p-1 transition-colors hover:bg-muted"
              >
                {/* eslint-disable-next-line @next/next/no-img-element */}
                <img src={s.imageUrl} alt={s.label} className="size-full object-contain" />
              </button>
            ))}
          </div>
        )}
      </PopoverContent>
    </Popover>
  );
}
```

- [ ] **Step 5: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 6: 커밋**

```bash
git add package.json package-lock.json src/components/ui/popover.tsx src/hooks/useStickers.ts src/components/trade/StickerPicker.tsx
git commit -m "feat: Popover UI·스티커 카탈로그 훅·StickerPicker 추가"
```

---

### Task 8: StockComments 통합 — 피커 + 렌더 + 단독 전송

**Files:**
- Modify: `src/components/trade/StockComments.tsx`

**Interfaces:**
- Consumes: `useStickers`(Task 7), `StickerPicker`(Task 7), 변경된 POST(Task 5), `StockComment.stickerId`(Task 4).

- [ ] **Step 1: import·인터페이스·상태 추가**

상단 import에 추가:

```ts
import { StickerPicker } from "@/components/trade/StickerPicker";
import { useStickers, type CatalogSticker } from "@/hooks/useStickers";
```

파일 내 로컬 `interface StockComment`에 필드 추가(`likedByMe` 다음):

```ts
  stickerId: string | null;
```

컴포넌트 상단 상태 추가(`const [content, setContent] = useState("");` 다음):

```ts
  const [sticker, setSticker] = useState<CatalogSticker | null>(null);
  const { byId } = useStickers();
```

- [ ] **Step 2: submit 로직 — 스티커 동봉·단독 허용**

`submit` 함수를 다음으로 교체:

```ts
  async function submit() {
    const trimmed = content.trim();
    if ((!trimmed && !sticker) || submitting) return;
    setSubmitting(true);
    try {
      await postJson(`/api/stocks/${stockCode}/comments`, {
        content: trimmed || undefined,
        stickerId: sticker?.id,
      });
      setContent("");
      setSticker(null);
      queryClient.invalidateQueries({ queryKey: ["comments", stockCode] });
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "댓글 작성에 실패했습니다.");
    } finally {
      setSubmitting(false);
    }
  }
```

- [ ] **Step 3: 입력부 — 피커 + 선택 미리보기**

입력 `<div className="flex gap-2"> ... </div>` 블록을 다음으로 교체:

```tsx
        <div className="flex flex-col gap-2">
          {sticker && (
            <div className="flex items-center gap-2 rounded-md border border-border/60 p-1.5">
              {/* eslint-disable-next-line @next/next/no-img-element */}
              <img src={sticker.imageUrl} alt={sticker.label} className="size-12 object-contain" />
              <span className="text-xs text-muted-foreground">{sticker.label}</span>
              <button
                type="button"
                onClick={() => setSticker(null)}
                aria-label="스티커 제거"
                className="ml-auto text-muted-foreground transition-colors hover:text-foreground"
              >
                <X className="size-4" />
              </button>
            </div>
          )}
          <div className="flex gap-2">
            <StickerPicker onSelect={setSticker} />
            <Input
              placeholder="한마디 남기기 (200자)"
              value={content}
              maxLength={200}
              onChange={(e) => setContent(e.target.value)}
              onKeyDown={(e) => e.key === "Enter" && !e.nativeEvent.isComposing && submit()}
            />
            <Button onClick={submit} disabled={submitting || (!content.trim() && !sticker)}>
              등록
            </Button>
          </div>
        </div>
```

(`X` 아이콘은 이미 import되어 있음.)

- [ ] **Step 4: 댓글 렌더 — 스티커 표시 + 스티커 단독 시 연필 숨김**

댓글 본문 렌더(`<p className="mt-0.5 break-words text-sm">{c.content}</p>`)를 다음으로 교체:

```tsx
                    {c.content && <p className="mt-0.5 break-words text-sm">{c.content}</p>}
                    {c.stickerId && byId.get(c.stickerId) && (
                      // eslint-disable-next-line @next/next/no-img-element
                      <img
                        src={byId.get(c.stickerId)!.imageUrl}
                        alt={byId.get(c.stickerId)!.label}
                        className="mt-1 size-24 object-contain"
                      />
                    )}
```

수정(연필) 버튼 조건을 `c.mine` → `c.mine && !!c.content`로 좁힌다(스티커 단독 댓글은 텍스트 수정 불가):

```tsx
                    {c.mine && !!c.content && (
                      <button
                        onClick={() => startEdit(c)}
                        aria-label="내 댓글 수정"
                        className="text-muted-foreground transition-colors hover:text-foreground"
                      >
                        <Pencil className="size-3.5" />
                      </button>
                    )}
```

- [ ] **Step 5: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 6: 실앱 verify (핵심 통합 지점)**

verify 스킬(dev + agent-browser)로 종목 상세에서 확인:
- 텍스트+스티커 댓글 작성 → 텍스트 아래 스티커 이미지 표시.
- **스티커만** 선택하고 등록(텍스트 없이) → 스티커만 표시.
- 스티커 제거(X) 동작.
- 스티커 단독 댓글엔 연필(수정) 버튼 없음, 삭제(X)는 있음.

- [ ] **Step 7: 커밋**

```bash
git add src/components/trade/StockComments.tsx
git commit -m "feat: 종목 토론방에 스티커 첨부·렌더·단독 전송 통합"
```

---

### Task 9: DiscussionList 렌더 — 토론뷰 스티커 표시

**Files:**
- Modify: `src/components/news/DiscussionList.tsx`

**Interfaces:**
- Consumes: `useStickers`(Task 7), `DiscussionComment.stickerId`(Task 4가 DTO에 포함).

- [ ] **Step 1: import·인터페이스·훅 추가**

상단 import에 추가:

```ts
import { useStickers } from "@/hooks/useStickers";
```

로컬 `interface DiscussionComment`에 추가(`stockName` 다음):

```ts
  stickerId: string | null;
```

`DiscussionList` 컴포넌트 상단(`const queryClient = useQueryClient();` 다음)에 추가:

```ts
  const { byId } = useStickers();
```

- [ ] **Step 2: 본문 렌더에 스티커 추가**

`<p className="mt-1 break-words text-sm">{c.content}</p>`를 다음으로 교체:

```tsx
          {c.content && <p className="mt-1 break-words text-sm">{c.content}</p>}
          {c.stickerId && byId.get(c.stickerId) && (
            // eslint-disable-next-line @next/next/no-img-element
            <img
              src={byId.get(c.stickerId)!.imageUrl}
              alt={byId.get(c.stickerId)!.label}
              className="mt-1 size-24 object-contain"
            />
          )}
```

- [ ] **Step 3: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 4: 실앱 verify**

뉴스탭 → 토론 세그먼트에서 스티커가 붙은 댓글이 이미지와 함께 보이는지 확인.

- [ ] **Step 5: 커밋**

```bash
git add src/components/news/DiscussionList.tsx
git commit -m "feat: 토론뷰에 스티커 렌더 추가"
```

---

### Task 10: 어드민 스티커 관리 섹션

**Files:**
- Create: `src/components/admin/StickerSection.tsx`
- Modify: `src/app/admin/page.tsx`

**Interfaces:**
- Consumes: `/api/admin/stickers`(Task 6), 기존 `getJson`/`postJson`/`patchJson`(`@/lib/api/client`), `deleteJson`(`@/lib/api/client`).

- [ ] **Step 1: StickerSection 작성**

`src/components/admin/StickerSection.tsx` — 목록(활성/비활성)·추가(로컬 파일 → base64)·활성 토글·삭제:

```tsx
"use client";

import { useState } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { deleteJson, getJson, patchJson, postJson } from "@/lib/api/client";

interface AdminSticker {
  id: string;
  label: string;
  imageUrl: string;
  sortOrder: number;
  isActive: boolean;
}

// 로컬 이미지 파일을 data URI 문자열로 읽는다.
function fileToDataUri(file: File): Promise<string> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => resolve(reader.result as string);
    reader.onerror = () => reject(new Error("파일을 읽지 못했습니다."));
    reader.readAsDataURL(file);
  });
}

export function StickerSection() {
  const queryClient = useQueryClient();
  const { data } = useQuery({
    queryKey: ["admin-stickers"],
    queryFn: () => getJson<{ stickers: AdminSticker[] }>("/api/admin/stickers"),
  });

  const [id, setId] = useState("");
  const [label, setLabel] = useState("");
  const [dataUri, setDataUri] = useState("");
  const [saving, setSaving] = useState(false);

  function invalidate() {
    queryClient.invalidateQueries({ queryKey: ["admin-stickers"] });
    queryClient.invalidateQueries({ queryKey: ["stickers"] }); // 공개 카탈로그도 갱신
  }

  async function onPickFile(file: File | undefined) {
    if (!file) return;
    try {
      setDataUri(await fileToDataUri(file));
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "파일 읽기 실패");
    }
  }

  async function add() {
    if (!id.trim() || !label.trim() || !dataUri || saving) return;
    setSaving(true);
    try {
      await postJson("/api/admin/stickers", {
        id: id.trim(),
        label: label.trim(),
        imageDataUri: dataUri,
      });
      setId("");
      setLabel("");
      setDataUri("");
      invalidate();
      toast.success("스티커를 추가했어요.");
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "추가 실패");
    } finally {
      setSaving(false);
    }
  }

  async function toggleActive(s: AdminSticker) {
    try {
      await patchJson("/api/admin/stickers", { id: s.id, isActive: !s.isActive });
      invalidate();
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "변경 실패");
    }
  }

  async function remove(s: AdminSticker) {
    if (!window.confirm(`'${s.label}' 스티커를 삭제할까요?`)) return;
    try {
      await deleteJson(`/api/admin/stickers?id=${encodeURIComponent(s.id)}`);
      invalidate();
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "삭제 실패");
    }
  }

  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-base">스티커 관리</CardTitle>
      </CardHeader>
      <CardContent className="flex flex-col gap-4">
        <div className="flex flex-col gap-2 rounded-md border border-border/60 p-3">
          <div className="flex gap-2">
            <Input placeholder="id (영소문자·숫자·하이픈)" value={id} onChange={(e) => setId(e.target.value)} />
            <Input placeholder="라벨" value={label} onChange={(e) => setLabel(e.target.value)} />
          </div>
          <input
            type="file"
            accept="image/png,image/jpeg,image/webp,image/svg+xml"
            onChange={(e) => onPickFile(e.target.files?.[0])}
            className="text-sm"
          />
          {dataUri && (
            // eslint-disable-next-line @next/next/no-img-element
            <img src={dataUri} alt="미리보기" className="size-20 object-contain" />
          )}
          <Button onClick={add} disabled={saving || !id.trim() || !label.trim() || !dataUri}>
            추가
          </Button>
          <p className="text-xs text-muted-foreground">이미지는 100KB 이하 png/jpeg/webp/svg</p>
        </div>

        <div className="grid grid-cols-2 gap-2 sm:grid-cols-3">
          {data?.stickers.map((s) => (
            <div
              key={s.id}
              className={`flex flex-col items-center gap-1 rounded-md border p-2 ${
                s.isActive ? "border-border/60" : "border-border/30 opacity-50"
              }`}
            >
              {/* eslint-disable-next-line @next/next/no-img-element */}
              <img src={s.imageUrl} alt={s.label} className="size-16 object-contain" />
              <span className="text-xs">{s.label}</span>
              <div className="flex gap-1">
                <Button size="sm" variant="outline" onClick={() => toggleActive(s)}>
                  {s.isActive ? "숨김" : "노출"}
                </Button>
                <Button size="sm" variant="outline" onClick={() => remove(s)}>
                  삭제
                </Button>
              </div>
            </div>
          ))}
        </div>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 2: admin 페이지에 편입**

`src/app/admin/page.tsx` import에 추가:

```ts
import { StickerSection } from "@/components/admin/StickerSection";
```

"manage" 탭 컨텐츠 안(다른 관리 섹션들이 렌더되는 `<TabsContent value="manage">` 블록)에 `<StickerSection />`을 추가한다. 정확한 위치는 파일에서 `TabsContent value="manage"`를 찾아 기존 섹션 나열 끝에 넣는다. 예:

```tsx
          <StockSection />
          <SectorSection />
          <StickerSection />
```

(해당 탭의 실제 섹션 구성은 파일 확인 후 맞춘다 — 새 섹션 한 줄만 추가.)

- [ ] **Step 3: 빌드·린트**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 4: 실앱 verify (배포 없는 추가 확인)**

verify 스킬로 어드민 로그인 → 관리 탭 → 스티커 관리:
- 로컬 PNG 선택 → 미리보기 → id·라벨 입력 → 추가 → 목록에 등장.
- 100KB 초과 파일은 추가 실패 토스트("100KB 이하").
- 종목 상세 토론방 피커에 방금 추가한 스티커가 **즉시** 노출(공개 카탈로그 무효화).
- 숨김 토글 시 피커에서 사라짐, 기존 댓글엔 계속 표시.
- 삭제 시 피커에서 사라지고 기존 댓글은 텍스트만 남아 안 깨짐.

- [ ] **Step 5: 커밋**

```bash
git add src/components/admin/StickerSection.tsx src/app/admin/page.tsx
git commit -m "feat: 어드민 스티커 관리 섹션 추가"
```

---

### Task 11: 최종 통합 verify + 배포 노트

**Files:**
- 없음(검증·문서 확인 태스크). 필요 시 `docs/DEPLOY.md` 스티커 배포 노트 한 줄 추가.

- [ ] **Step 1: 전체 빌드·린트 최종 확인**

Run: `npm run build && npm run lint`
Expected: 통과.

- [ ] **Step 2: end-to-end 실앱 verify**

verify 스킬로 시나리오 통과 확인:
1. 어드민이 스티커 2~3개 추가(배포 없이 즉시 반영).
2. 일반 유저로 텍스트+스티커 / 스티커 단독 댓글 작성.
3. 토론뷰(뉴스탭)에서 스티커 렌더.
4. 비로그인 상태에서 스티커 이미지는 보이되 작성 시 로그인 유도.
5. 스티커 비활성/삭제 후 기존 댓글 무손상.

- [ ] **Step 3: 배포 체크리스트 확인(문서화)**

개장(7/28) 전 배포 절차([[rehearsal-reset-before-open]]·[[sector-overhaul-deploy-lessons]]):
- 코드 배포(main 머지 → Vercel) 먼저.
- 마이그레이션 `20260718080000_stickers.sql` **prod push**.
- 리허설 재생성(reset는 stickers 무수정 — 카탈로그 보존). prod엔 seed 미적용이므로 **어드민에서 실제 스티커 등록** 필요.
- 시뮬 재검증 불필요(현금가치 0).

- [ ] **Step 4: 최종 커밋(문서 변경이 있으면)**

```bash
git add -A
git commit -m "docs: 스티커 배포 노트 추가"
```

---

## Self-Review

**Spec coverage (스펙 §별 → 태스크 매핑):**
- 데이터 모델(stickers 테이블·stock_comments 확장·content 완화·has_body) → Task 1. ✅
- 카탈로그 서비스(list/create/update/setActive/delete/assertValid·100KB·형식검증) → Task 2. ✅
- 공개 카탈로그 API(`GET /api/stickers`, imageUrl 추상화) → Task 3. ✅
- 댓글 서비스 확장(stickerId·DTO·select·최소 하나) → Task 4. ✅
- 댓글 API(content optional·stickerId·refine) → Task 5. ✅
- 어드민 CRUD API → Task 6. ✅
- 카탈로그 훅·StickerPicker(Popover 신설 포함) → Task 7. ✅
- StockComments 통합(피커·단독 전송·렌더) → Task 8. ✅
- 토론뷰 렌더 → Task 9. ✅
- 어드민 관리 페이지(배포 없는 추가·활성 토글·삭제·미리보기) → Task 10. ✅
- 검증·배포(build/lint·실앱 verify·prod push·리허설·시뮬 무관) → Task 11. ✅
- 리허설 reset 무수정 근거 → File Structure 노트. ✅

**Placeholder scan:** 코드 스텝은 모두 실제 코드 포함. Task 10 Step 2의 "TabsContent value=manage 위치 확인 후 한 줄 추가"는 실행자가 파일에서 확인해야 하는 실제 통합 지점(placeholder 아님). Task 1 Step 3 psql 접속은 선택(무오류 reset로 갈음 명시).

**Type consistency:**
- `createComment(userId, stockCode, content: string | null, stickerId: string | null)` — Task 4 정의 = Task 5 호출부 인자 순서·타입 일치. ✅
- `CatalogSticker { id, label, imageUrl }` — Task 7 정의 = Task 8·9 사용 일치. `Sticker.imageUrl`(서비스) → API에서 `{id,label,imageUrl}`로 축약 → `CatalogSticker` 동일 필드. ✅
- `StickerInput { id, label, imageDataUri, sortOrder? }` — Task 2 정의 = Task 6 createSchema(`{id,label,imageDataUri,sortOrder?}`) 일치. ✅
- `StockComment.stickerId: string | null` — Task 4 = Task 8 로컬 인터페이스 일치. `DiscussionComment`도 동일 필드(Task 9). ✅
- Popover export(`Popover`/`PopoverTrigger`/`PopoverContent`) — Task 7 정의 = StickerPicker 사용 일치. ✅
