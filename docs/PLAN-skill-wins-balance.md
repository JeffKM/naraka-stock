# 실력자 우승 밸런스 — 실제 반영 계획

> 목표: **"단타 아닌, 정보를 읽는 실력자가 최종 총자산 상위 4명(=상품)에 들 확률 1위"**.
> 근거·검증: 밸런스 하네스(`scripts/simulate.ts`) 몬테카를로 + 크라운 토너먼트. 풀틱(4,320) 재검증 완료(2026-07-20).
> 관련 메모리: skill-wins-balance-harness.

## 0. 검증된 결론 (요약)

- **진단**: 단일 총자산 크라운은 평균이 아니라 "오른쪽 꼬리(최대값)"를 보상 → 변동성 몰빵이 이김. 시장이 ~10배 우상향이라 변동성=순수 상방. 현행에선 **랜덤 단타조차 크라운 42%**.
- **해법(풀틱 검증)**: 도박 꼬리를 죽이고(잡주 하방 + 매도수수료) 스킬 신호를 키우면(섹터강도 + 종목뉴스 채널), 실력 전략(교차검증)이 상위4의 ~69%를 차지, 블라인드·도박은 전멸.
- **핵심 원리**: 스킬 = 톤/소문을 "읽는" 게 아니라 **초반 시세 브레이크로 확인(교차검증)**. 그래서 방향을 애매하게 둘수록 게으른 블라인드만 죽고 확인하는 실력자는 멀쩡(톤 단독 51% → 교차검증 진입 81%).

## 확정 파라미터 (2026-07-20, config A)

Phase 0 완화 스윕 결과 채택. 검증된 가혹값(drift −1.5·점프30·수수료2%)이 잡주 손실 99.7%로 과해, 실력 리드는 유지하면서 잡주 복권성을 살리는 완화값으로 확정.

| 레버 | 파일/위치 | 현행 → 확정 |
|---|---|---|
| 섹터강도 `SECTOR_MAGNITUDE` | `src/lib/engine/bias.ts` | 15 → **25** |
| 잡주 드리프트 `DAILY_DRIFT.wild` | `src/lib/engine/randomWalk.ts` | −0.2 → **−1.0** |
| 잡주 점프상승확률 `JUMP_UP_PROBABILITY.wild` | `src/lib/engine/randomWalk.ts` | 0.5 → **0.35**(35:65) |
| 매도수수료 `sell_fee_bp` | `config` (마이그레이션) | 50 → **150**(0.5%→1.5%) |

검증(288틱·필드72명): 종목뉴스교차검증 상위4 42.6%·소문교차검증 26.5%(실력 69%), 단타 상위4 16.7%(=기준선), 잡주몰빵 상위4 7.1%·손실 95.7%·최대 2.9배(복권성 보존). σ 위계(0.005/0.009/0.015)는 불변 → 등급 차별화 유지.

## 롤아웃 원칙

1. **별도 브랜치**에서 진행(main 직접 수정 금지).
2. **단계별 위험×노력 순**: 기계적 "실력 우위" 수정을 먼저(작고 저위험), 콘텐츠 큰 종목뉴스 채널은 후속.
3. **섹터 소문 교차검증 채널은 이미 존재** → Phase 1–2만으로도 실력자가 상위4 리드(하네스 config D 기준 소문교차검증 상위4 39%). 종목뉴스(Phase 3)는 "두 번째 스킬 채널 + 피드 활기" 부가.
4. **env 오버라이드 패턴 유지**: 엔진 상수는 `SIM_*` env 미설정 시 운영값. 반영은 이 "운영 기본값"을 바꾸는 것.
5. 이벤트 미개장(8/1)이라 엔진 파라미터는 **다음 배치 사전생성분부터** 반영 — 라이브 충격 없음.

---

## Phase 0 — 준비 & 완화 스윕 (하네스, 반나절)

가혹함 완화가 목적. 풀틱 검증값(잡주 drift −1.5·점프 30:70·수수료 2%)은 **잡주몰빵 손실 98%**로 체감이 너무 셀 수 있음("내 잡주는 맨날 떨어짐").

- [x] 브랜치 생성 `feat/skill-wins-balance` (origin/main 기준, 하네스 커밋 `463d1ed`)
- [x] **완화 스윕 완료(2026-07-20)** → config A 확정(위 "확정 파라미터" 표)
- [x] 결정 ① 잡주 하방 = drift −1.0 / 점프 35:65 (완화, 복권성 보존)
- [x] 결정 ② 수수료 = 정액 1.5% (1% 이하로 내리면 단타 상위4가 기준선 위로 부활 → 1.5%가 바닥)

## Phase 1 — 밸런스 레버 (엔진 TS, 저위험, 무콘텐츠) ✅ 완료(2026-07-20)

순수 TS 엔진 상수. 다음 배치 경로부터 반영, 마이그레이션 없음. 커밋 `fc2bfa3`.

- [x] `src/lib/engine/bias.ts` — `SECTOR_MAGNITUDE` 15 → **25**
- [x] `src/lib/engine/randomWalk.ts` — `DAILY_DRIFT.wild` −0.2 → **−1.0**, `JUMP_UP_PROBABILITY.wild` 0.5 → **0.35**
- [x] `SIM_*` env 오버라이드 코드 유지(운영 기본값만 변경) + simulate 헤더 표시 동기화
- [x] 검증: `npm run simulate` 재확인(헤더 새 값 로드 확인). 로컬 배치·차트 sanity는 Phase 4 게이트로 통합

## Phase 2 — 매도 수수료 (config + UI, 플레이어 대면) ✅ 완료(2026-07-20)

함수 수정 불필요 — `config.sell_fee_bp` 값만. 커밋 `6a18c8e`.

- [x] 마이그레이션 `20260720050000_sell_fee_150bp.sql`: sell_fee_bp 50 → **150** (로컬 DB 적용 확인)
- [x] UI 문구 동기화: `guide/page.tsx`, `TradePanel.tsx`(`SELL_FEE_RATE 0.015` + "수수료 (1.5%)")
- [x] PRD `docs/PRD.md` 수수료 규칙 1.5% 갱신
- [ ] 검증: 로컬 거래 체결 후 수수료 표시=서버 계산 일치 → Phase 4 게이트로 통합

> ⚠️ **검증 정정(2026-07-20):** "Phase 1+2만으로 실력 우위 달성" 전제는 **틀렸다**. 하네스 배포필드(종목뉴스 제외)에서 Phase 1-2 단독은 상위4에서 단타(27.9%)를 못 이기고 소문교차검증은 기준선(18.9%)≈블라인드. 실력 우위의 실질 견인은 **Phase 3a 종목뉴스 채널** → 사장님 결정 ③ = **B(Phase 3 묶어 반영)**.

## Phase 3 — 종목뉴스 채널 (결정 B로 반영) — 3a 완료 / 3b 연기

**하네스 재검증(2026-07-20)으로 3a/3b 분리 확정:** 헤드페이크(펌프-덤프)는 결정 B 목표에
**불필요**하고 난이도 조절용이다. 배포필드 상위4 점유 — 헤드페이크0: 실력 68.3%·단타 6.4%,
0.3: 실력 40.4%·단타 23.0%, 0.5(과함): 실력 18.5%·단타 32.6%. 즉 헤드페이크 없이도 실력채널이
단타/도박을 압도 → **3a(엔진 무변경·기존 재활용) 먼저 반영**, 헤드페이크(가격엔진 변경·비가역)는
3b로 분리해 3a 실앱 체감 후 필요 시.

### Phase 3a — 종목 초반 톤뉴스 채널 ✅ 완료(커밋 `cadecd6`)

기존 조기 방향뉴스(top-2·70%·방향표시)를 하네스 검증 모델로 확장. 엔진 변경·신규 콘텐츠 없음.

- [x] `generate.ts`: `pickEarlySignalTargets`/`generateEarlySignalNews` → `pickStockNewsTargets`/`generateStockEarlyNews`
  - 진짜 = `|개별 bias|≥20`, 필러 = 편향0 종목 진짜수×1.0 (진짜:노이즈 1:1)
  - 장 초반 **0~40% 창 분산 발행**, 방향 미표기(톤이 진짜 방향을 **0.6 정확도**로만 힌트)
  - grade "news" 유지, 진짜·필러는 후반 정식뉴스에서 제외(excludeCodes)
- [x] `batchService.ts` 배선, 톤 = 기존 `HINT_TEMPLATES` 방향버킷 0.6정확도 재활용(신규 문안 불필요)
- [x] 거래량 단서 = 실제 틱 거래량(시세/차트에 이미 노출) → 별도 저장/마이그 불필요
- [x] 검증: tsc·eslint·전체 build 통과, 스모크(선정·창·1:1), 하네스(헤드페이크0 실력 68%)
- [ ] (선택) UI: 뉴스 피드에 거래량 칩(발견성) — 시세/차트로 이미 확인 가능해 3a는 생략

### Phase 3b — 헤드페이크(펌프-덤프 함정) + 거래량 단서 강화 (연기, 선택)

난이도용. 유일한 비가역·가격엔진 변경. 3a 실앱 체감 후 "실력채널이 너무 압도적(68%)"이면 도입.

두 번째 스킬 채널 + 중반 피드 활기. 위험·노력 최고.

- [ ] **선행 조사(필수)**: `src/lib/news/generate.ts`의 기존 **`EARLY_SIGNAL_ACCURACY` / `excludeCodes` / 조기 방향뉴스** 스캐폴딩 파악 → 신규 구현이 아니라 이걸 **재활용/확장**. (지금 비활성/부분구현 여부 확인)
- [ ] 엔진/배치: 종목 단위 초반 뉴스 생성 — `|개별 bias|≥20` 진짜 + 방향성0 필러(진짜:노이즈 1:1), **0~40% 창 분산 발행**, **방향 미표기(톤만, 정확도 ~0.6)**. 반전 노이즈는 기존 `realizeBias` FLIP_PROBABILITY 0.3 그대로
- [ ] 타이밍 재분배: `tailNewsTick`(강제 후반) → **각 종목 움직임 "직후"**(선취 0)로 확인 뉴스 분산 + **거래량/색깔 뉴스**(경로 파생)로 중반 피드 채우기 → 초반 몰림/중반 공백 해소
- [ ] **헤드페이크(펌프-덤프 함정) + 거래량 단서** (2026-07-20 하네스 검증): 종목뉴스의 일부(진짜당 **~0.3**)를 "초반에 확 튀어 순진한 가격확인을 통과 → 종가엔 꺼지는" 함정으로. **단서 = 거래량**(진짜=거래량 실림, 함정=대개 얇음, **정확도 ~0.8=불완전**). 스킬이 "초반 오르는 거 보고 사기"(1-factor)에서 **"거래량 실린 진짜 상승 vs 세력 펌프 함정 종합판단"(다층)**으로 깊어짐. 검증: 순진 추종 적중 45%·최종 0.20×(박살), 거래량 실력 적중 70%·상위4 41%. **함정률 0.5는 과함(채널 net-손해→단타 득세), 0.3이 스위트스팟.** 브라켓 exploit 안 열림(펌프-덤프 예측불가).
  - ⚠️ **다채널종합(섹터+종목뉴스 교차대조)은 드롭**: 가격 교차검증이 이미 내리는 멤버를 걸러 추가가치 없음(하네스서 소문교차검증과 동률). 각 채널은 각자 스킬이지만 둘의 "충돌 해소"는 별도 스킬 아님.
- [ ] 콘텐츠: `src/lib/news/templates.ts`에 **애매한 톤 헤드라인**(방향 안 드러나게) 라이브러리. 세계관 캐논 준수(naraka-lore-canon), 사장님 검수 필요
- [ ] UI: 뉴스 피드가 속보를 방향 라벨 없이 표시 + **거래량 표시**(단서), 하루 종일 밀도 유지 확인
- [ ] 검증: 하네스(완료) + 로컬 배치 후 **피드 시간분포 확인** + 실앱 verify(verify 스킬)

**결정 필요 ③**: Phase 3를 이번에 포함 vs Phase 1–2만 먼저 ship(섹터 채널로 충분). 권장 = 1–2 먼저 ship 후 3은 후속.

## Phase 4 — 리허설 & 검증

- [x] **로컬 배치 1회 검증(2026-07-20):** date=2026-07-31 배치로 08-01 생성(틱 181,440·뉴스 36, POST 200/25s). 결과:
  - 3a 채널: 0~40% 창에 16건(진짜~8+필러~8) 집중 = 설계대로. 종목당 뉴스 1건·early↔tail 중복 0(excludeCodes 정상)
  - Phase 1: wild 일중 평균 −4.9%(하방 틸트) but 최대 +24.7%·상하한(±30%) 도배 0 = 복권성 보존·급락 과다 아님
  - Phase 2: sell_fee_bp=150 적용 확인. (테스트 08-01 데이터는 원복 완료)
- [ ] 실앱 verify(verify 스킬 — 리허설 클록 셋업 필요: 08-01 데이터를 현재시각 장중화) — 뉴스 피드 톤뉴스 렌더 + 수수료 1.5% 표시 확인
- [ ] 리허설 데이터 초기화(rehearsal-reset-before-open) 후 로컬 플레이스루로 "실력 vs 도박" 체감
- [ ] PR → prod 배포(사장님 자격증명, sector-overhaul-deploy-lessons 절차)

---

## 위험·롤백

- 엔진 파라미터(Phase 1)는 배치 재생성으로 즉시 롤백 가능(상수 되돌리고 배치 재실행).
- 수수료(Phase 2)는 config 값 되돌리는 마이그레이션 1줄.
- 종목뉴스(Phase 3)가 유일한 비가역 콘텐츠 작업 — 그래서 마지막·독립 단계.

## 미해결 결정 요약

| # | 결정 | 상태 |
|---|---|---|
| ① | 잡주 하방 강도 | ✅ 확정: drift −1.0 / 점프 35:65 (반영 완료) |
| ② | 수수료 값·방식 | ✅ 확정: 정액 1.5% (반영 완료) |
| ③ | Phase 3 포함 시점 | ✅ 확정: **B(묶어 반영)**. 3a 완료·3b(헤드페이크) 연기 |

## 현재 상태 (2026-07-20)

브랜치 `feat/skill-wins-balance` — 커밋 4개(미푸시): 하네스 정리 `463d1ed` / Phase1 `fc2bfa3` /
Phase3a `cadecd6` / Phase2 `6a18c8e`. tsc·eslint·build 통과.
**Phase 4 로컬 배치 검증 완료** — 3a 채널·Phase 1 차트·Phase 2 수수료 전부 실배치에서 정상 확인
(위 Phase 4 섹션 상세). **남음:** 실앱 verify(리허설 클록) → PR → prod 배포(사장님 자격증명).
